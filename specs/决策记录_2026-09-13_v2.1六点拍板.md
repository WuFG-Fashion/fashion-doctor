# 决策记录：v2.1 §13.9 六点拍板落地 + aliases 覆盖审计（2026-09-13）

> 铁律⑪：每个关键决策一行「决策 + 为什么」。
> 决策来源：`specs/知识库v2架构方案_讨论稿.md` §13.9「六个关键点 · 我的推荐答案」，老板 2026-09-13 逐条批准（候选 B）。

## 1. 六点拍板与落地

| # | 决策（老板批准） | 为什么 | 落地动作 |
|---|---|---|---|
| ① | **保留 T0-T4** | 管"知识角色"（谁压谁），与 confidence（数据可信度）正交，不合并 | CLAUDE.md 2.1 layer 注释扩为全档 + 五档目录映射（T0 规则/T1 判断/T2 事实/T3 原料/T4 视图） |
| ② | **个人域完全独立索引** | `retrieval: never` + 不进公司索引，物理上不可能泄漏 | 扫描范围不含 20_personal（既有）+ kb_updater 双保险（既有）+ 检索端跳过（既有）+ **--inject 对 never/scope=personal 拒绝注入（新增代码红线）** + 隔离验证工具 |
| ③ | **T4 允许显式注入** | 不进向量召回但可显式附加，否则看板生成后用不上 | `retrieval_mod.py` 新增 `load_explicit()` + `--inject <路径>` CLI；T4 视图（Home.md）retrieval: explicit_only 可注入 |
| ④ | **公司隔离仅 RAG**（非文件 ACL） | 单主人私有仓库，文件 ACL 是过度工程；元数据已预留升级路径 | 40_companies/dongshang **40 页全量铺 frontmatter**（type: practice / layer: T3 / scope: company / company: dongshang / evergreen / as_of: 未知 / review_due_at: 2027-03-12=落盘日+180d）；master_index 只扫 30_wiki，公司域永不进共享索引 |
| ⑤ | **语义层真源=「当前主版本」指针** | 代码+测试+真实消费方在系统侧；KB 只读镜像；升级只改一行 | 新建 `knowledge_base/config/semantic_source.yaml`（version_pointer: current / resolved: dongshang-v3 / mirror_status: pending——04_语义层 镜像 P4 落地）；CLAUDE.md 架构树 + §七 增加指针引用铁律 |
| ⑥ | **物理迁移落定** | 决定架构生效的是元数据+索引+检索策略，不是目录名 | **口径说明**：§13.9⑥ 原文"无限期延后"，但八分区物理迁移已于 2026-09-12 执行（`specs/决策记录_2026-09-12_v2.1物理改造落地.md`）——本条落地为"**迁移已成终态，此后不再安排任何物理迁移**"，架构演进一律走元数据/索引/检索层 |

## 2. 新增工具

- `knowledge_base/tools/_v21_isolation_check.py`：隔离与检索准入验证（幂等只读，可复跑）。
  - [1] master_index 零 20_personal/ 零 40_companies/ 条目 ✓
  - [2] 检索时门禁零泄漏（never/explicit_only/expired/retired 均被 search_index 跳过）✓
  - [3] --inject 四象限：T4 explicit_only=允许 / 个人域 never=拒绝 / 路径越界=拒绝 / 公司域 eligible=允许 ✓
  - [4] 公司域 fm 覆盖 40/40 ✓
  - **终判 PASS（2026-09-13）**

## 3. aliases 覆盖审计（同日执行）

- **口径**：编译层（30_wiki 六目录 1417 页）frontmatter `aliases` 非空审计；实体/概念页为 CLAUDE.md 5.1 硬门槛，practices/playbooks 为补齐目标，模板页豁免。
- **缺口 12 页**：practices×4 + playbooks×6（实页）+ 模板×2（`_template.md`、`决策日志_模板.md`，豁免并登记）。
- **处理**：10 实页按标题推断补齐中/英/别称（AI 推断口径，手写映射非自动生成）；实体/概念页审计确认 100% 达标（零缺口）。
- **验证**：索引重建后 aliases 非空 **1413/1415 = 实页 100%**；别名检索实测「清货→清仓决策树」「私域流量→私域运营方法论」「MD企划→商品企划落地打法」均走"别名含"命中路径。
- `90_meta/` 7/40 有 aliases 属正常（meta 页非召回对象）；`10_web` 907 页无 aliases 属正常（原料层不直接召回）。

## 4. 验收（按 5.1 验收铁律）

- `_b11_audit.py` 复跑：**终审判定 PASS**（A 存在性=True / B 空体=True / C 引导语=0）——aliases/fm 编辑不触碰三区块。
- 普通检索回归：`retrieval_mod.py 清仓 --compact` 正常返回 3 来源（含清仓决策树）。
- 索引重建 1415 条（kb_version 3.1），`_v21_isolation_check.py` PASS。
- 换行/编码：脚本二进制读写 + 按文件探测 CRLF/LF + utf-8 严格→gbk 兜底，公司文件含 GBK 兼容处理，零转码。

## 5. 执行坑记录

- **同文件并行 Edit 竞态**：两条 Edit 并行改 `_v21_isolation_check.py`，后写整体覆盖先写（[4] 修复存活、[3b] 修复被冲掉），导致首轮验证假 FAIL——同文件编辑必须串行。已记入用户级跨项目坑清单。
- 验证脚本首版按前 700 字节判公司 fm：UTF-8 多字节字符被截断→严格解码失败→误走 GBK→19/40 假阴性。改全文件读取后 40/40。**截断读+双编码兜底是反模式，验收一律全量读。**

## 6. 提交

1. `[落地] v2.1 §13.9 六点拍板落地` — CLAUDE.md + retrieval_mod.py + _v21_isolation_check.py + config/semantic_source.yaml + 40_companies/dongshang 40 页 fm
2. `[补齐] aliases 覆盖审计：编译层 10 页缺口补齐（模板 2 页豁免）` — 30_wiki 10 页 + master_index.json
3. `[记录] v2.1 六点决策记录 + memory` — 本文件 + 90_meta/log.md + .workbuddy/memory/

push 后 `git ls-remote origin main` HEAD == 本地 HEAD 核对。
