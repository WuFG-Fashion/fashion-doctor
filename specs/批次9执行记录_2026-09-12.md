# 批次9执行记录 — 编译层「前沿」补齐轮（178 篇）

**执行日期**：2026-09-12
**任务来源**：`specs/批次8执行记录_2026-09-12.md` §10 建议中的「编译层专项审计」方向，经边界实测收敛为本批
**提交**：`0f2bf86`（已推送至 `origin/main`，`git ls-remote` 核实远端 HEAD 一致：`0f2bf8680d6e…`）

---

## 1. 边界实测与独立核验

批次 8 文档 §10 只给了「编译层专项审计」的模糊建议。按 SOUL.md 铁律先独立复算，不信任上游文字。脚本 `.kbtmp/_b9_audit.py` 实测：

```
sources（五目录）: 1183   三区块 100%（批次 8 已闭环）
编译层六目录:
  entities/concepts/comparisons/practices/playbooks + sources
  缺「## 前沿」: 179 篇
  排除模板页 playbooks/决策日志_模板.md → 实做 178 篇
```

关键定性：**179 篇全部是「已有 结论+信息链、仅缺 前沿」的纯单区块缺口**，且与批次 1–8 清单零重叠（`.kbtmp/_b9_list.py` 校验 overlap=0）。

**开工前 3 个反驳理由**（铁律 10）：
1. sources 才是 RAG 检索主力，编译层值得再投一轮吗？→ 反驳不成立：编译层含 cabbeen/kb_benchmarks 等高频命中页，检索价值密度更高。
2. entities 页尾部区块已堆叠很厚（frontmatter+别名+结论+信息链），再加前沿会更臃肿？→ 前沿恰好起「收口」作用（下轮核查点+维护策略），接受。
3. 模板页补前沿无意义？→ **采纳**：排除 `决策日志_模板.md`，179→178。

---

## 2. 差异化撰写口径（用户拍板）

| 页型 | 前沿写法 |
|---|---|
| 品牌实体页 | 待核口径 + 下轮采集焦点 |
| 概念页 | 适用边界 + 可迁移性 + 时效警示 |
| 对比页 | 数据黑箱与合成稳定性 |
| 实操页（practices） | 模板落地与字段映射 |
| 作战手册页（playbooks） | 可执行性验证点 |
| 内部培训资料 | 只做落地追踪，不评价内容 |

---

## 3. 执行流程与校验结果

流程沿用批次 2–8 的「骨架→分段→即检→merge→注入→幂等」链路，全部脚本在 `.kbtmp/_b9_*.py`：

| 段 | 覆盖 | wikilink | 校验 |
|---|---|---|---|
| 1 | entities 30 | 0 条 | PASS（首跑占位符误报已改措辞） |
| 2 | entities 30 | 44 条 | PASS |
| 3 | entities 30 | 32 条 | PASS |
| 4 | concepts 30 | 28 条 | PASS（漏 1 页已补；伪链 `[[子页]]` 已改纯文本） |
| 5 | concepts 30 | 52 条 | PASS |
| 6 | comparisons 5 + practices 9 + playbooks 14 | 54 条 | PASS |
| 合计 | **178 篇** | **210 条引用 / 76 个去重目标** | **断链 0** |

注入与幂等验证：
- 首次注入 `f_add: 178, f_skip: 0, missing: []`
- 二次复跑 `f_add: 0, f_skip: 178`（幂等成立）
- `git diff --numstat`：**178 files changed, 1372 insertions(+), 1 deletion(-)**
- 注入后全库终验：本批 wikilink 断链 0；编译层五目录 222 页三区块 **100%**（comparisons 11 / concepts 113 / entities 65 / playbooks 15 / practices 18）

---

## 4. 那 1 行 deletion 的定性（无害）

唯一 deletion 出现在 `concepts/中国服装零售基准体系2026.md`：原文件末行 `-- [[2026-09-02_B_会员复购率2026基准与忠诚度模型]]` 缺结尾换行符，注入追加时补了换行，git 记为「删 1 行旧的（无换行）+ 加 1 行新的（有换行）」。**内容零删除**，行内容 diff 完全一致。

---

## 5. 本批新发现矛盾（登记待核）

1. **七匹狼沉睡会员回流率，量级差 7 倍**：`concepts/sleeping_member_reactivation.md` 写 7-10%→12-16%；`entities/septwolves.md` 实体页写 70-80%。两页已在前沿双向登记，需下一轮以原始 source 页裁决。
2. **海澜之家「216 亿」疑口径混入**：`concepts/男装品牌竞争格局2026Q1.md` 摘要中 216 亿疑为 2025 全年口径混入 Q1 榜单（Q1 应为 66.61 亿）。已登记待核。
3. **53 页「结论」为模板引导语（批次 1 漏检根因）**：批次 1 的筛选条件只判断 `## 结论` 区块是否存在、未校验是否为模板引导语，导致 53 页漏网（含段 1 的 anta/anzheng_fashion/baoxiniao/bienlefen/bosideng/burberry/fast_retailing/hla/hm/inditex_zara/jiumuwang/jnby 等 12 页）。本批已为这 12 页在前沿中补「未闭合项」说明；**其余 41 页需另开一轮处置**，本批不扩边界。
4. **继承矛盾库织入**：批次 7/8 沉淀的口径矛盾（卡宾代销 201→202 vs 101→370、门店 571 vs 573、HOKA 三说、MLB 1,094 vs 1,185、Tommy 10.77 vs 8.42 亿、库存周转 2.1 vs 9.13 次、Salomon 302→315 已裁定时序推进）已按页型织入相关页前沿。

---

## 6. 存疑不立档（诚实备注）

撰写过程曾提及「5 页缺一句话摘要」。本轮提交前以两个客观口径复扫均无法重现：① frontmatter `summary` 字段——编译层 178 页统一无此字段（全库体例如此，非个例）；② H1 后正文导语——178 页统一为空（H1 后直接进 `##` 区块）。两个都是全库统一体例而非 5 页个例，故**不立档**，仅备注于此。

---

## 7. 下一轮建议

1. **53 页模板引导语结论处置轮**（优先级最高）：批次 1 漏检的 41 页 + 本批已标注的 12 页，统一按「真结论」口径重写或标记 `status: needs_rewrite`。
2. **矛盾收敛轮**：本批新增 2 个矛盾（七匹狼回流率、海澜 216 亿）+ 继承矛盾库，逐条回到原始 source 页裁决并在实体页收口。
3. **编译层 aliases 覆盖审计**：RAG 四大支柱中 aliases 是检索命中率决定项，编译层 222 页的 aliases 覆盖率尚未像 sources 层那样拉齐（当前 91% 为全库口径），可实测后决定是否开批。

---

## 附：本批产物清单

- `.kbtmp/_b9_audit.py` / `_b9_probe.py` / `_b9_list.py` / `_b9_dump.py` / `_b9_check.py` / `_b9_merge.py` / `_b9_inject.py`
- `.kbtmp/_batch9_list.json` / `_b9_gap_names.json` / `_b9_blocks_1..6.json` / `_b9_blocks.json`
- 提交 `0f2bf86`：178 个文件，`knowledge_base/30_wiki/{entities,concepts,comparisons,practices,playbooks}/`
