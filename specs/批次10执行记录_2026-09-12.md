# 批次10执行记录 — 模板引导语结论处置轮（53 篇）

**执行日期**：2026-09-12
**任务来源**：批次 1 样板页遗留的模板引导语结论（`## 结论` 仍为「⏳ 待 AI 合成洞察」引导语的 53 页），批次 9 记录「下一轮任务」收口项
**提交**：`5d9d403`（53 files，+195/−369）

---

## 1. 本批与批次 9 的本质差异：追加式 → 替换式

| 维度 | 批次 9（前沿补齐） | 批次 10（结论重写） |
|---|---|---|
| 操作类型 | **追加式**：文末加 `## 前沿` | **替换式**：定位 `## 结论` 标题到下一个 `^## ` 之间**整体替换** |
| 正则 | 无需定位（追加） | `^## 结论\r?\n(.*?)(?=^## )`（re.S+re.M） |
| 幂等判定 | `if "## 前沿" in t: skip` | 结论区块含「待 AI 合成洞察」→ 替换；否则 skip |
| 附带清理 | 无 | 前沿区块中「**本页另有未闭合项**」过时标注行（有则删、无则跳） |
| 产物格式 | `## 前沿\n\n` + 前沿文本 | `## 结论\n\n` + 编号判断列表 + `\n\n` |

---

## 2. 独立核验（不信任上游名单）

通过两份骨架（`.kbtmp/_b10_skeleton_1.txt` / `_b10_skeleton_2.txt`）+ 全量 dump（`_b10_dump_1.txt` 3627 行 / `_b10_dump_2.txt` 2561 行）逐页核对素材，
确认 53 页 = **concepts 28**（骨架 1）+ **entities 25**（骨架 2），每页结论区块均实测含引导语标记。

**样板页抽查**：`concepts/服装行业竞争格局.md`、`entities/cabbeen.md` 的成品结论格式（空行 + 编号判断）作为格式基线。

---

## 3. 引号新规（本轮确立）

批次 1 样板页结论含 **6 个 ASCII 直引号 U+0022**（历史遗留反例，Python Counter 实测）。
本批起**强制使用直角引号「」**，校验器新增禁 U+0022 项（批次 9 校验器的 `'"' in v` 检查升级为 c 字段强制检查）。

---

## 4. 分段撰写与七项校验

53 篇分两段（骨架 + 分段即时校验，沿用批次 8/9 流程）：

| 段 | 范围 | 篇数 | 校验结果 |
|---|---|---|---|
| 1 | concepts（ETL 选型 … 男装竞争格局 2026Q1） | 28 | PASS（23 条 wikilink 断链 0） |
| 2 | entities（anta … 深维智信） | 25 | PASS（50 条 wikilink 断链 0） |

校验器 `.kbtmp/_b10_check.py` 七项（较批次 9 五项新增两项）：

1. keys 顺序严格等于骨架
2. c 字段非空
3. **无英文双引号 U+0022**（强制直角引号「」）
4. wikilink 回查 `_stems.json` 断链 0
5. 无占位符
6. **无引导语残留**（「待 AI 合成洞察」「本页另有未闭合项」）
7. **无二级标题嵌套**（c 字段内禁 `## `）

---

## 5. 页型差异化口径

- **entities 服务商页**（丽晶 / 探马SCRM / 深维智信 / style3d_lingdi / suhao_fashion）：结论 = **选型判断**（服务边界、PoC 必要性、自宣置信度），非品牌经营判断。
- **fast_retailing**：按前沿既定说明处理为**概念型实体页**，结论 = SPA 模式适用边界（勿超配，与湖仓/ETL 选型同构）。
- **top_sports**：零售运营商（财报口径），按经营判断写，标注「投资视角 vs 运营视角」分流。
- **概念页**：适用边界 + 决策框架，禁数据复述。

---

## 6. 注入与幂等（`.kbtmp/_b10_inject.py`）

| 轮次 | c_replace | c_skip | front_clean | 判定 |
|---|---|---|---|---|
| 首次 | **53** | 0 | **47** | 符合预期（6 页前沿本无标注行，如安奈儿） |
| 复跑 | 0 | **53** | 0 | **全 skip → 幂等成立** |

---

## 7. git 核对与提交

- `git diff --numstat`：54 files = **53 个 KB 文件**（全部带 deletions，删的是引导语/标注行）+ 1 个今日 memory（+15，非本批产物）
- 暂存复核：`git diff --cached --name-only` = 53，**0 文件超出** `knowledge_base/30_wiki/{concepts,entities}/`
- 样本抽查（septwolves）：引导语整块替换为 4 条合成判断；前沿仅删标注行、保留待核口径与采集焦点
- 提交 `5d9d403`：53 files changed，**+195/−369**

---

## 8. 矛盾口径登记（写入结论的裁定）

| 矛盾 | 裁定口径 | 落在哪些结论 |
|---|---|---|
| 七匹狼沉睡回流 7-10%→12-16% vs 狼族俱乐部 70-80% | 窄/宽双口径分层，不可混引；与 [[sleeping_member_reactivation]] 双向登记 | septwolves |
| 海澜摘要 216 亿 vs 正文 66.61 亿 | 216 亿 = 2025 全年口径混入摘要，以正文为准（段 1 hla 结论登记） | hla |
| 慕尚净利 +2.3% vs 营收 -9.4% | 节流式增长（三费省 1.79 亿 > 毛利降 1.51 亿），非业务复苏 | muson_gxg / hm（跨国互证） |
| 深维价格异议 +17% vs kb_benchmarks +18% | 数值不一致待验证，引用以页内为准并标注待核 | 深维智信 |
| MO&Co. 82%/51% vs kb_benchmarks 15%/28%/12% | 口径不同（自然年老客回购 vs 180 天严口径），禁直接横比 | moco_epo / jnby |
| 比音勒芬 25.4% vs 12.77% | 单季峰值 vs 全年口径，Q4 波动已解释，引用须区分 | bienlefen |
| 苏豪 110+ 品牌 vs 智尚云 50 余家 | 两个客户口径（AI 智造累计 vs 云平台），引用注明 | suhao_fashion |
| 归母 vs 扣非撕裂三样本 | 九牧王（投资浮亏）/ 朗姿（补税）/ 安正（低基数）三种成因分立 | jiumuwang / langzi_fashion / anzheng_fashion |

---

## 9. 下一轮任务建议

1. **三区块 100% 全库终审**（推荐）：concepts/entities 的引导语缺口已闭合，对全库 30_wiki 做一次终审快照（结论含引导语 = 0），归档批次 1 漏检根因。
2. **第 9 节矛盾清单回写源页**：批次 8 清单（TH 口径、卡宾代销、太平鸟门店方向等）仍待回写。
3. **S 轮照常**：53 页新结论为跨品牌合成提供了比引导语时代质量高一个量级的输入。
