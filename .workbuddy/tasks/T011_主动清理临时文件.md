# T011: 主动清理临时文件

## 任务来源
图灵（蜂后）2026-04-26 统一下发

## 清理原则（红线，必须遵守）
以下文件类型**禁止删除**（无论是否看起来是临时文件）：
- `*.py` 核心业务脚本（retail_analysis_v3.py, retail_knowledge_base.md 等）
- `cabbeen*.db` 数据库文件
- `.workbuddy/` 目录下的任何文件
- 知识库文件（*.md）
- 有实质性内容的 `MEMORY.md` / 日志文件

## 清理标准
以下文件**可以清理**：
- 以 `_` 开头的临时调试脚本（如 `_check_*.py`, `_debug_*.py`）
- 明显的临时测试文件（如 `test_*.py`, `temp_*.py`）
- 重复的任务文件（同一任务多个版本，只保留最新版）
- 无效的输出文件

## 执行要求
1. **先列清单**：列出所有候选清理文件（路径 + 大小 + 修改时间）
2. **按清单清理**：逐个确认后删除，不要用批量删除
3. **不动不确定的**：凡有疑问的文件，先跳过，在 reply 里标注

## 交付物
在 `.workbuddy/tasks/reply_T011.md` 回复：
1. 清理了多少个文件，释放了多少空间
2. 跳过了哪些文件（原因）
3. 更新的 MEMORY.md（如有）

## 截止时间
2026-04-26 12:00 前完成
