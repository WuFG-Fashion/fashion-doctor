# 2026-09-12

## 00:40 — 架构核对报告复核 + v2 方案升级 v2.1（仅改 specs，零知识库文件变动）

- 老板转来一份架构核对报告（疑另一 AI 产出）。按铁律先实证复核其全部现状判断（Python 全量扫描，shell coreutils 第二次退化、全程用 Python 绕过）。
- **复核结论：报告约六成对四成错**。对的：新字段覆盖全 0（1400 页）、自动化 7 份含 optimize（方案原漏）、brand_configs 仅 peacebird.toml、自动化 `git add knowledge_base/` 违 CLAUDE.md L284「禁止整目录提交」。错的：raw/articles 实为 101 非 759、同名交集实测 0 非 595、磁盘 0 个 L2 目录（非 414 md）、wiki/_archive 存在但空壳。
- **新发现（双方都漏）**：CLAUDE.md L28 声称「383 历史快照归档于 wiki/_archive/l2_history/」——该目录 0 个 md，归档从未落盘，规则文档撒谎。
- 方案新增 **§13 v2.1 修订**（优先级最高）：13.1 事实核正表；13.2 frontmatter 11 字段数据契约（type/layer/scope/company/volatility/as_of/review_due_at/expires_at/status/superseded_by/retrieval；layer 管"知识角色"与 confidence 分离）；13.3 TTL 三轨拆分（expires_at / review_due_at / superseded_by，retired 仅人批）；13.4 双链只校验四件事 + ## 前沿合并待办 + 信息链机器真源唯一；13.5 RAG 隔离在建索引前（独立 collection + 隔离测试）；13.6 T4 不进向量召回但显式注入；13.7 语义层真源=V3、KB 只读镜像；13.8 执行顺序改七步；13.9 六个关键点推荐答案（待老板拍板）。
- 待老板拍板六点：①保留T0-T4 ②个人域独立索引 ③T4显式注入 ④公司隔离=仅RAG（现阶段）⑤语义层真源=V3 ⑥物理迁移无限期延后。
- 00:50 — 老板点破「他说的是git，你说的是本地仓库？」→ 三方对照定案（git 历史 / git 索引 / 磁盘）：**审核报告扫的是 git 历史**。历史中两处 raw 曾有 753 个同名（审核的 595 由此来）、L2 有 809 个路径（其"414 md"由此来）；现状磁盘/索引同名交集=0、L2=0。两个定性改写：①原料问题降级为「入口分裂」（wiki/raw 是从根 raw 迁出的、根 raw 剩 101 篇旧入口残余，非内容重复）；②_archive 空壳严重性下调（git 历史可随时找回，CLAUDE.md L28 只需修文案）。方法论教训：我第一遍 git ls-files 忘加 -c core.quotepath=false 漏掉全部中文名文件（wiki/raw 误报 54，实际 783），重跑修正——老坑复踩，施工前一律「磁盘递归 + quotepath=false 三方对照」。§0/§13.1 已更新为三视角定案版。
- 07:50 — **老板拍板：六点全过 + 七步序通过；唯一纠偏=语义层真源不钉死 V3**（未来有 V4/V5）→ specs §13.7 改「东尚系统当前主版本」指针机制（config 里 version_pointer: current 恒定，resolved 行随升级改，规则引用 resolved 视为违规）。**随即开工七步序第 2 步并完成**：
  - `kb_updater.py`：条目注入 11 个契约字段（type/layer/scope/company/volatility/as_of/review_due_at/expires_at/lifecycle/superseded_by/retrieval）；retired 不入索引；个人域+临时收集强制 retrieval=never 双保险；levels.L2 描述去掉 legacy 假账；kb_version 3.0→3.1 + field_contract 块；WIKI_MAP 加 type、LAYER_DEFAULTS（source/entity=T2，其余=T1 提示值）。
  - `retrieval_mod.py`：search_index 生命周期门禁——lifecycle in (expired,retired) 或 retrieval in (never,explicit_only) 默认不召回；存量页无字段=放行，向后兼容。
  - 验证：重建 1407 条（含今晨 A1/A2/A3 新增 7 页）；冒烟检索「复购率」10 条正常；伪造 retired/never/expired 三类条目全部拦截 ✅。手误一次：genexp 里 k 未绑定导致 exit 1，已修。
  - git：bb3509f 精确提交 4 文件（L284 合规）并 push。
- 07:55 — **.git refs 幽灵问题第 3 次复发**：push 成功但 origin/main 读回 6 月旧值，refs/remotes/origin 目录又被删（昨天 20:38 修过一次）。本次双层修复+加固：makedirs + Python 手写 loose ref（先误写占位哈希弄脏 ref，删除重写修正）+ `git pack-refs --all` 把正确值 bb3509f 焊进 packed-refs——即使 loose 再被删，回退值也正确。已写入 ~/.workbuddy/MEMORY.md 跨项目踩坑。**元凶仍未抓到**（删除发生在两次会话之间的某个时刻；3 秒存活测试通过，非实时删除），建议后续留意哪个同步/清理工具扫 .git。
- 上午（阶段2 收尾）— **提示词修改完成并提交 55b50b0**，push 后 `git ls-remote` 权威核对 GitHub main = 本地 HEAD = 55b50b0。CLAUDE.md 6 处 + A1/A2/A3/B/C 各 4 处 + S 2 处（optimize 经核已合规零改动），共 7 文件 115+/12-。要点：①造页强制新增 7 契约字段（layer/scope/volatility/as_of/expires_at|review_due_at/status；as_of=观测时间非写作时间）②废除 5.1/5.2「至少一条双链/禁止孤岛」硬门槛 → R1 信息链完整 + R3 无断链 + 新增 `## 前沿` 区块 ③git add 整目录→精确路径（修 L284：轮次规范四子路径 raw/wiki/_health/__index__，S 收窄三子路径）④1082→1400+ 条目口径（kb_version 3.1）⑤CLAUDE.md L28 归档假账修正（_archive/l2_history 空壳 → 改述为 git 历史可找回）。
- 上午（refs 翻案）— **".git refs 幽灵删除"当场取证改判**：push 后 origin/main 读回旧值 bb3509f → 现场为 refs/remotes/origin 目录不存在 + packed-refs 持旧值 + `git update-ref` rc=0 **静默零写入**；而 Python 裸 os.makedirs+手写 loose ref 秒成、git 立即可读、8 秒存活、packed-refs 未被动过。**新结论：大概率从未存在"删除进程"**——目录"消失"=引用全部 packed 后的正常形态；真异常 = 沙箱下 git.exe 无法在 .git/refs/remotes/ 新建 loose ref（静默失败），fetch/push 的跟踪引用更新落不了盘，rev-parse 回退读 packed 旧值，伪装成"被删/被回滚"。处置：Python 手写 loose ref 修复至 55b50b0，**故意不 pack-refs**（保留 loose 文件，让后续 push/fetch 走"覆写已有文件"路径，覆写可行性由 commit 更新 refs/heads/main 实证）。同步核对权威手段 = `git ls-remote origin main` 直问远端。~/.workbuddy/MEMORY.md 对应条目已同步翻案。
- 10:40 — **阶段2 查漏补修 + 系统侧 prompt 同步 + 审计报告更新**：老板要求"检查这次修改还有没有问题"→ 自查发现 55b50b0 不完整（上轮 EBUSY/Obsidian 回写致编辑静默丢失）：A1/A2/B/C 整目录 git add 残留 12 处、A2/A3/B/C 孤岛硬规则残留 4 处、CLAUDE.md 计划 6 处只落 3 处。补齐后提交 **dd9a1bc**（7 文件 26+/22-，ls-remote 核对一致）。**根因确认：Obsidian/编辑器内存缓冲区回写覆盖外部编辑，Edit 报成功但被冲掉；对策=原子写+立即回读校验+重试循环（vault 内文件与被编辑器打开的文件必用）**。随后系统侧 8 个知识库自动化 prompt 全部同步 v2.1（步数范围→第十步、废"禁孤岛"措辞、契约字段、精确 git add；optimize 改文件驱动指向 `_automation_optimize.md`；A1/A2/A3 名称去「镜头」改「品牌全维度」；ingestInbox=ec8090b6 prompt 全文升级——此前唯一未同步的自动化）。最后更新桌面《自动化任务复刻清单与执行审计报告_2026-08-19.md》：ID 全部改 UUID 制式、任务3 标 DELETED（总数 11→10）、§三 prompt 全文刷新、新增第九节 v2.1 修订摘要；桌面文件同样遭回写（自动保存），用 `C:\tmp\fix_report.py` 原子重写 18 处+残留扫描清零。备份：`C:\tmp\自动化任务复刻清单与执行审计报告_2026-08-19.bak.md`。工作区 MEMORY.md 同步校正（自动化 ID、v2.1 已拍板状态、七步序进度、RAG 第四支柱改连通性）。

## 06:50 A1 轮（自动）

- A1 固定分组 12 品牌全维度采集：**4 真新增 / 8 显式核验**；索引 1404 L3（+4）；0 断链 / 0 孤岛 / 0 新增矛盾。
- 新增 4 源：chuu（政瞳战报 GMV 876.33 万/礼盒追加 2000 套）、dkny（鞋类授权商 DBI FY2026 Q2 财报，授权分层实证）、ellesse（美国回归渠道细化 + RepSpark B2B 第六条通道）、dekashell（旭弘实业工商核验：2022-09-02 成立/法人朱建纲/注册资本 300 万）。
- 提交：c0670fe（前半程）+ c0cda60（后半程），已推送 main。
- 待用户决策：awoken_space / awoken_time 建议降级为季度探针。
- 工具坑记录：本机 Git Bash heredoc 含中文长文本会报 unexpected EOF → 改用 `.kbtmp/` 临时脚本执行。

## A2 轮（07:05）— 1 真新增 / 10 显式无新增

- **新增源**：`wiki/sources/2026-09-12_A2_karl_lagerfeld_G-III_Q2电话会KL全球经营与酒店管线.md`（confidence 财报）
  - KL 全球段：北美批发领跑 / 欧洲销售承压但毛利率扩张；KL PARIS 连衣裙+鞋履动能最强；KL JEANS 持续跑赢（年轻客群）
  - **酒店/住宅管线首次量化**：1 酒店 + 1 住宅已开业、5 在开发；里斯本 KL Residences 2026-06
  - H2 营销 = Paris Hilton 第 3 季 + 全球 NOT-KARL campaign（09-03 源由此升格）
- **新概念沉淀**：品牌墙「**品牌变现深度三级梯度**」= 卖商品（mlb）→ 卖场景（lacoste 酒店胶囊）→ 卖资产（KL 酒店/住宅）；新增风险类型「**虚拟 IP 依赖度**」（NOT-KARL 无销售侧归因）
- **判定纪律（新增）**：D2C 内容农场文（未证实 ROAS/团队姓名不符）一律不入库 —— 本轮 etudes 命中即拒
- **索引** 1405 L3（+1）；⚠️ 数据矛盾 57 页与 09-06 基线持平；分段提交 b845e1a → 25bc4c8（已 push，本地=origin）
- **待办**：kb_benchmarks A2 组 11 品牌条目仍全为 `{}`；king_baby 检索词需重设；marcelo 建议降频月度探针；mlb_kids 建议并入 mlb 子项

---

## A3 轮（07:20）— 固定分组 12 品牌全维度采集

- **结果**：**2 真新增 / 10 显式核验**（非静默跳过）。索引 **1407 L3**（+2）；⚠️ 数据矛盾 57 页 66 处（= 09-06 optimize 基线未变）；断链 0（19 页 2289 条双链实测）、孤岛 0。分段 2 次提交 `4b13252`（前半程）→ `0bbb498`（后半程，已 push，本地=origin/main）
- **新增 2 源**（均 官方公告 / brand_specific:true）：
  - **peacebird** 半年度业绩说明会**当日问答口径**（9/11 15:00-16:00，记录表 2026-002）：管理层**首次显式归因**"营收 -0.72% 主要受加盟收入下降影响"（直营 +2.73% / 线上 +3.06% / **加盟 -10.32%**）；**库内新数据** 经营现金流 2.41 亿 + 期末现金 2.63 亿；行业定性"**内生优化的精益经营新阶段**"；门店口径"旗舰店/集合店/单品牌店为核心"（**旗舰与集合店 114 家逆势净增 +10**）；**股东回报** 累计派现募资比 232.32%（行业 7/65）+ 2026 中期**不分配不转增**
  - **nautica** **家居窗饰品类授权**（SelectBlinds × Nautica Home，仅其官网独家；对手方 2023 被 Hunter Douglas 收购）+ Nautica"70+ 品类"+ **ABG 底座**（1,700+ 授权商 / 150 国 / 年系统零售额超 $380 亿 / 50+ 品牌）
- **新概念沉淀（4 条，均入 [[服装行业竞争格局]] 与对应概念/对比页）**：
  1. **「加盟失速 vs 直营/线上正增长」= 品牌力体检结构** → 加盟制品牌应盯"加盟商单店进货额"而非门店数
  2. **门店判断用店型净变化而非总店数**（peacebird 净关 137 家中旗舰/集合店 +10）
  3. **品牌变现新增第四档「卖品类」**（跨行业授权，不与服装景气度相关）→ 接 A2 轮"卖商品→卖场景→卖资产"三级
  4. **档案资产化新增第三型「自身广告物再造」**（tommy 1980s Times Square 广告牌印上长背心裙 / trussardi 档案裙 / levis RED 三型对照）
  5. 新增风险型「**加盟渠道单点失速**」+「**股东回报口径混用**」（历史累计 vs 当期政策）
- **待办**：peacebird 说明会问题 6（股价支撑答复）需查公告 PDF 原文；**the_mr_young 连续 12 轮污染**（卡宾/2AM 路径亦失败）→ 建议正式降频为月度核验；mr_mrs/no_one_else/thisisizi8 建议重设检索词；kb_benchmarks A3 组 11 品牌条目仍为 `{}`

---

## optimize 轮（11:36-11:45）— lint 五规则 + 织网 + 索引重建 + 基准受控刷新

- **lint 终态**：断链 **11 → 0**（实修 11 处）· 孤岛 **0**（入链 1407/1407 = 100%）· 矛盾 **0 新增**（⚠️ 全库 57 页/66 处与 09-06 基线逐字对上）· 过期 139（口径处置）· 分类错 **0** · 出链 26,476 条。
- **断链根因（本轮唯一 P0 上游问题）**：A1 轮对 8 个品牌（adlv/ariose_years/awoken_space/awoken_time/cabbeen/crocs/dickies/diesel）判定「显式核验无新增、不造 source 页」，但实体页 cross_refs 写了 `[[2026-09-12_A1_<brand>_核验登记]]` 指向一个从未创建的页面 → 8 处幻影链。已删该 cross_refs 条目（核验内容本就内联在各实体页 `## A1轮核验登记（2026-09-12）` 小节）。另 3 处为错误日期/截断/合并源链（dekashell 08-31→08-21、dkny 09-04 补全、ellesse 08-30 改指美国市场回归），均按实际文件名重指。
- **织网（§3.5 只补自然链接）**：孤岛虽为 0，但连通性分析发现 6 个遗留页 `cross_refs: []` 为空且仅 1 条语义入链 → 补 **21 条自然链接**（16brands_2026q1_snapshot +4 / ai_policy +3 / bosideng_fy2026_deep_dive +3 / vogue_business_ai_consumer_2026 +3 / croquis +4 / gxg +4），每页附「织网说明」注明语义依据，未为凑连通性造伪链。连通性分布：1源16 / 2源77 / 3源166 / 4源163 / **5+源985**，**0 页仅靠 index.md 登记**。
- **索引重建**：`kb_updater.py` → master_index.json **kb_version 3.1 · 1407 L3**（entities 65 / concepts 113 / sources 1183 / comparisons 11 / practices 18 / playbooks 17）。
- **基准受控刷新**：`scripts/update_benchmarks_controlled.py --apply` → dry-run 拟新增竞品 0 个；仅刷 updated / meta.last_scan / meta.updated 至 09-12（meta.updated 上轮遗漏，本轮补同步）；**剥除元数据键后全文件 JSON 序列化与刷新前逐字节一致 → 零阈值改动（脚本证明）**。
- **过期 54 → 139 不是退化**：90 天 cutoff 随日期前移（09-06 时 cutoff=06-08，09-12 时 =06-14），06-09~06-14 区间静态源页整批跨阈值；静态源页「数据快照不判罚」口径不变。6 个维护型过期页（three_brands_mid2026 / china_apparel_2026q1_operations / china_apparel_industry_scale_2026 / 服装采购渠道选型2026 / 男装品牌竞争格局2026Q1 / 探马SCRM）建议下轮评估 superseded_by 或刷新。
- **产物**：`_health/2026-09-12_daily_health_optimize.md` 快照 + `wiki/log.md` 追加行。
- **git**：分段 2 段（无 relations 独立变更）→ `73fe3c9`（修复段 17 页）+ `dfd3d22`（产物段 4 文件），已 push；local HEAD == origin/main == dfd3d22；知识库待提交 0。
- **下轮建议（已写入快照）**：①在 `_automation_A1/A2/A3.md` 第九步加硬约束「核验登记只写实体页内联小节，禁止 cross_refs 指向独立核验登记页」——从根上防幻影链复发；②维护型过期页收口；③ℹ️ 基准核对 129→156 处应定性为「自洽标注数」而非疑似矛盾数。

---

## 11:30 — v2.1 物理改造（三步走第三步）全部落地并收尾

- **step6 批次提交 6f73abe 并推送 origin/main**（524fbcd..6f73abe 共 8 个 commit；回滚点 tag `pre-physical-migration` 已推）。物理六步全落地：step1 字段回填 2190 页 → step2 死层退役 50_legacy → step3 raw 合并 10_web → step4 八分区物理成型 → step5 tools/自动化规范/CLAUDE.md 路径同步 → step6 Home 脚本化（tools/build_home.py）+ 全量验证。
- **验证结论**：索引稳定 1407 L3 全程不变；断链扫描 2023 md / 27136 链接 / 107 "broken" 逐一核查全为既有模板占位符（CLAUDE.md/log.md/_template.md 文档示例），**迁移引入真断链=0**；检索冒烟 confidence=high。报告：`knowledge_base/90_meta/_health/2026-09-12_v21_physical_verification.md`；决策记录（8 条）：`specs/决策记录_2026-09-12_v2.1物理改造落地.md`。
- **自动化全部恢复 ACTIVE**（10/10）；并发现系统侧 7 个自动化 prompt 内嵌路径仍是 raw/wiki 旧路径（step5 只改了规范文件）→ 已同步改为 10_web/30_wiki/90_meta。
- **遗留**：107 条既有模板占位符"断链"另立清理批次；培训课件 322 篇归属（v2 未决问⑤）仍未拍板。

---

## 13:47 — 老板问「自动化能否更准搜集/提炼/存放对应路径」→ 实查出 step5 引入的双重前缀路径 bug 并修复

- **实查结论**：tools 脚本路径常量全对（INDEX_FILE=90_meta/__index__、wiki_root=30_wiki、RAW_DIR=10_web、log=90_meta/log.md），但 **7 份规范文件里 step5 批量替换引入 35 处双重前缀错误**：
  - `30_30_wiki/index.md`（应为 30_wiki/index.md）、`30_90_meta/log.md`（应为 90_meta/log.md）、`90_meta/90_meta/_health/` 与 `90_meta/90_meta/__index__/`（应为 90_meta/_health/、90_meta/__index__/）。
  - 根因：wiki/ 批量替换时前缀重复叠加。若不修，明早 A1 轮会把 log 写到错误目录 `30_90_meta/`、健康快照写到 `90_meta/90_meta/`。
  - `_automation_optimize.md` 与 `CLAUDE.md` 无此错误（干净）。
- **修复**：`.kbtmp/fix_paths.py` 精确字符串批量替换（3 类规则），A1/A2/A3/B/C/S 共 35 处 → 提交 `f862f9a` 已推 origin/main。grep 验证残留=0，正确路径抽样通过。
- **教训**：批量路径替换后必须 grep「双重前缀」类异常模式（如 `30_30_`、`_90_meta`、`90_meta/90_meta`）做残留扫描，不能只信 diff 行数。

## 14:06 — 老板问「这次是否只改架构/现有md要不要改/新旧md有无区别」→ 量化出存量补齐缺口

- **实证**：30_wiki 共 1409 md（=索引1407 + index.md + _template.md）；**「## 前沿」区块覆盖 0 页**；「## 结论」702 页、「## 信息链」713 页（均≈50%，缺的都是 sources 里迁来的旧 article）。
- **结论**：物理改造只动 frontmatter 头部（2190 页批量补 layer/scope/volatility/as_of/status 等推断值），正文零改动；但旧页「前沿」0 覆盖、结论/信息链 ~50% 覆盖 → **存量未拉齐 v2.1 新规范**，属「另立存量补齐批次」的遗留缺口（与 107 断链、322 课件并列）。
- **留意隐患**：回填时历史 source 页出现 `expires_at` 早已过期（如 as_of=2021-03-04 → expires_at=2021-06-02）但 `status: active` 的矛盾；检索门禁目前只看 status/lifecycle 不看 expires_at，暂不影响召回，但语义上应补「expires_at 已过 → 软化」的判定规则。

---

## 14:15 — 老板要「全部补齐」→ 实查出更严重的 step3 前缀 bug 并修复（这轮信息量最大）

- **老板诉求拆解**：① 全部补齐（结论/信息链/前沿/断链/过期规则）② 说人话解释 frontmatter 与「采集时作者主动填准确值」怎么操作。
- **实查推翻之前「107 条模板占位符断链」的结论**：旧 lint 脚本 `kb_lint_5rules.py` 的 WIKI 变量在 step5 被改成 KB_ROOT（knowledge_base 根），但 resolve_page 候选路径还是旧目录结构（缺 30_wiki 前缀）→ 旧脚本在新路径下系统性误报。重写正确断链扫描（30_wiki 六目录 + 全库 stem 匹配）后，真相是：**step3 迁移残留了 ~619 处 `wiki/` 旧前缀**（`[[wiki/10_web/articles/xxx]]`、`wiki/sources/xxx`、`wiki/wiki/10_web/xxx` 等），这才是真断链（内容页 57 条）。
- **修复动作**：
  1. `retrieval_mod.py` 补 `_is_expired()` + search_index 过期软化降权（expires_at 已过 → score 打 3 折 + 标 expired），commit `52b9f6f`。影响面：1407 条里 140 条（10%）过期页降权。
  2. `fix_wiki_prefix.py` 批量清理 wiki/ 旧前缀 488 处（299 文件），**内容页断链 57 → 0**。映射：wiki/10_web→10_web、wiki/raw→10_web、wiki/{sources|entities|concepts|comparisons|practices|playbooks}→30_wiki/xxx、wiki/log.md→90_meta/log.md 等。
  3. **自纠两个误伤**：清理脚本的 `wiki/xxx/→30_wiki/xxx/` 规则误伤已正确的 `30_wiki/xxx/`（`30_wiki` 含 `wiki/` 子串）→ 产出 22 处 `30_30_wiki/`（CLAUDE.md 20 + brand_wall/README 2），已用 fix_3030.py 修回；CLAUDE.md 的 `wiki/_archive/ 空壳已移除` 被误改成 `50_legacy/ 空壳已移除`（语义错误），已 Edit 恢复。另回退 _health/2026-08-15 快照（Obsidian 自动格式化表格，非本批改动）。
- **遗留（另立批次，本轮不硬做）**：① 结论/信息链 705 篇（659 sources+46 编译页）缺区块——海量智力活需分批 ② 「## 前沿」1409 篇 0 覆盖——不能批量造空壳，应「边用边补 + 允许暂无」机制 ③ 原料 10_web 层 89 处 wiki/ 残留（含相对路径 ../wiki/ 与外部 URL ima.qq.com/wiki，需单独小心处理）④ 历史快照 _health+log 里 63 处旧路径描述（历史事实，保留不改）。
- **铁律教训（重要，复用）**：批量字符串替换 `wiki/xxx/ → 30_wiki/xxx/` 这类「前缀→新前缀」规则，**必须用负向后顾或先保护已正确的新前缀**，否则 `30_wiki/xxx/` 里含 `wiki/xxx/` 子串会被二次替换成 `30_30_wiki/xxx/`。这正是 step5 也出 `30_30_wiki` 的同一个根因——两次踩同一个坑，下次替换前先 grep 新前缀是否已存在于目标文件。

---

## 15:15 — 老板纠正 frontmatter 口径 + 要求产出存量补齐方案（不执行）

- **口径纠正（重要）**：frontmatter 标签**由 AI 推断填**，不是"采集时作者主动填准确值"；**确定不了/明确未知 → 填 `未知`**；老板自行修正需要修正的字段。已写入项目 MEMORY.md 长期约定 + 方案文档 2.1 节。
- **产出**：`specs/存量补齐批次方案_2026-09-12.md`（自包含，供独立对话执行），含：① 修改要求（frontmatter 口径 + 5 条补齐铁律 + 三区块填写规范）② 执行步骤（结论/信息链/前沿合并遍历 + 原料层 wiki 残留甄别 + 元文件断链收尾）③ 执行批次（10 批：批1 编译层 46 篇 → 批2-7 sources 659 篇分 6 批按时间倒序 → 批8 前沿 702 篇 → 批9 原料层 89 处 → 批10 元文件断链）。
- **精确数字**：1407 内容页缺结论 705 / 缺信息链 694 / 缺前沿 1407（全缺）。
- **本轮不执行**，等老板在另一个对话说"开始执行存量补齐批次 N"。

---

## 15:22-16:20 — 存量补齐 批次1 执行完成（编译层 44 篇）→ commit 1c0ff3b 已推送

- **老板指令**：按 `specs/存量补齐批次方案_2026-09-12.md` 逐步执行；先给 3 个反驳理由再动手（SOUL 铁律10）→ 提出 3 处方案问题，老板全过 → 开始执行批次1。
- **批次1 实做 44 篇**（方案写 46，排除 2 个写作模板 `playbooks/_template.md`/`决策日志_模板.md`）：entities 2 + concepts 28 + comparisons 4 + practices 9 + playbooks 1。补结论 44 / 信息链 43 / 前沿 44（`商品企划落地打法.md` 原已有信息链）。
- **改动**：44 files changed, **594 insertions(+), 3 deletions(-)**。3 处"删除"经逐字符验证=末尾补换行的同一行"删+加"，**原文 100% 保留**。
- **执行中修正方案 3 处**：① 排除 2 模板 ② **新增空行分隔修复**（14 篇页面末尾缺 trailing newline → `## 结论` 与前文粘连，已全量补空行并校验 44/44 合规）③ 自引入断链 1 处（`decision_log_template` → 实为 `决策日志_模板`）已修。
- **断链扫描**：本次新增 **383 条 wikilink，断链 0**（全库现存 39 条均历史遗留）。git 精确路径暂存（越界 0/漏 0），push 后 `git ls-remote` 核对远端 main = 本地 HEAD = `1c0ff3b` ✅。
- ⚠️ **重要发现·修正方案批次8 口径**（实测交叠分析，推翻我先前判断）：
  - 「缺结论且有信息链」= **0 篇**（先前担心的"53 篇只缺信息链会漏"**不存在**，风险撤销）
  - 「缺结论但有信息链」= **10 篇**（全在 sources，会被批次2–7 的"缺结论"筛命中，不构成遗漏）
  - 因此 **批次8 真实口径 = 「结论+信息链都齐、仅缺前沿」的 746 篇**，非方案写的 702（差额 44 = 批次1 刚补好的编译层页）
  - 批次2–7 的"缺结论"总数实测 **661**（非方案 659），差额 2 = 那 2 个模板，后续按"缺结论"筛时须显式排除
- **产出**：`specs/批次1执行记录_2026-09-12.md`（含全部数字、3 处修正、批次8 口径更正、验收清单）。
- **工具坑**：本机 shell coreutils 第三次退化（ls/grep/dirname/cd 全不可用）→ 全程 Python subprocess 绕过；git 用绝对路径 `PortableGit\versions\1.2.0\cmd\git.exe`；中文文件名必须 `-c core.quotepath=false` + `-z`（NUL 分隔），否则 `--name-only` 输出带引号转义无法 open。
- **下一批**：批次2 = sources 缺结论按文件名日期倒序取最新 ~110 篇。

---

## 16:00-16:35 — 存量补齐 批次3 执行完成（sources 次新 110 篇）

- **批次边界**：批次2 后剩余 549，批次3 = [0:110]，首 `2026-08-09_山西证券_纺服中期策略海外需求与板块估值` → 末 `2026-07-26_Megaview即时反馈训练开口焦虑期缩短`。**批次4 首篇 = `2026-07-26_Megaview需求挖掘转化提升23%`**。
- **完成**：110/110 篇三块齐全（结论110/信息链110/前沿110，文件缺失0，幂等跳过0）；顺序异常0；**断链=0**；diff=110 文件全部在清单内、清单外0；暂存精确 110。
- **唯一"删除行"说明**：`2026-08-07_单款店均效率SPS与款式宽度反推模型.md` 原文件末尾**无换行符**，注入器补末尾换行 → diff 显示同一行 1 删 1 加，**内容 100% 保留**（与批次1/2 同类现象）。
- **本批处理 2 类问题**：① 撰写区块时杜撰了 5 个不存在的 wikilink 目标（`服装私域会员运营`/`服装零售下沉市场`/`data_engineering_stack_2026`/`inventory_health_metrics_2026`/`零售销售力体系`），**落盘前**用全库 stem 集校验并替换为真实页（→ `会员与VIP运营体系2026`/`服装企划趋势渠道`/`python_data_stack_decision_2026`/`apparel_inventory_benchmark_2026`/`retail_sales_force_system`），故注入后一次断链=0。**教训：区块必须在落盘前做全库目标校验，不能等注入后再扫**（批次2 曾因此产 30 处断链）。② `_b2_inject.py` 要求 `_b2_blocks.json` 键**含 `.md`**（`os.path.join(W,fn)`），而 `_batch3_list.json` 存名不带后缀 → 首次跑 missing=110，补后缀后成功。
- **工具坑复现**：`.kbtmp/_b2_dump.py` 第13行硬编码 `_batch2_list.json`，误用它导出批次3 会取到批次2 已完成的页面（显示"三块齐全"）→ 已建 `_b3_dump.py`（清单可传参 + 自动补 .md + 显示三项 flag）替代。
- **产出**：`specs/批次3执行记录_2026-09-12.md`（含边界、验证表、5 个链接替换映射、4 条下批护栏）。
- **下一批**：批次4 = 缺结论 sources[110:220]，批次3 后剩 439 篇。


---

## 16:53-17:20 — 存量补齐 批次4 执行完成（sources 第三段 110 篇）

- **批次边界**：批次3 后剩余 439，批次4 = [0:110]，首 `2026-07-09_Danilchenko_DuckDB_vs_Polars_2026基准` → 末 `2026-06-21_Streamlit_2026_H2_Starlette正式化`。清单 `_batch4_list.json`（**带 .md**，110 项）。
- ⚠️ **修正批次3 记录的错误**：批次3 记录称「批次4 首篇 = `2026-07-26_Megaview需求挖掘转化提升23%`」**不正确** —— 它其实是**批次3 自己的第 110 篇**（index [109]，即批次3 末篇位置）。根因：批次3 执行时边界记录偏移一位。**后续批次一律以「新清单与旧清单零重叠」为接续判据，不信执行记录里的首/末篇文字描述。**
- **完成**：110/110 篇三块齐全（结论110/信息链110/前沿110，缺失0，跳过0）；顺序异常0；**断链=0**；diff=110 文件全在清单内、清单外0；**1425 insertions / 0 deletions**（本批零删除行，比批次3 干净）；幂等复跑 c_add=0/l_add=0/f_add=0、skip=110/110/110、files=0。
- **本批处理 4 类问题**：
  1. **分段生成日期边界重叠**：区块分 4 个 part 生成（40/50/60/20 = 170 条），part2 与 part3 在 06-25/06-24 系列重叠 → **10 条同名 DUP**（内容一致无害）；合并脚本按白名单过滤，丢弃 50 条非本批超生成页，最终 merged=110/missing=0/bad=[]。**教训：分段必须按清单 index 切，不按日期区间切。**
  2. **Python 裸双引号 SyntaxError**：把原文 `duckdb.sql("...").pl()` 原样写进 `"c": "..."` → `SyntaxError`；改写为文字描述。**新区块正文一律禁用裸双引号，中文引号用直角引号「」。**
  3. **4 个虚构 wikilink**：`sql_query_optimization_2026`→`sql_optimization`；`2026-07-27_经济观察报_618消费洞察与AI零售逻辑`→`2026-06-27_...`（日期错）；`服装SKU精细化管理`→`sku_fine_management`；`沉睡会员唤醒策略`→`sleeping_member_reactivation`。**后两个与批次3 同型：误把 concept 页的中文标题当成文件名，实际是英文 slug —— 遇到中文标题类目标必须回 `_stems.json` 反查真实 slug。**
  4. `_b4_gen3.py` 顶部残留 3 行 `None` 占位，被 `if k in B` 保护未污染输出，冗余无害。
- **工具**：新注入器 `_b4_inject.py`（由 `_b2_inject.py` 复制 + 切输入为 `_b4_blocks.json`），未改 `_b2_inject.py` 本体。合并脚本 `_b4_merge.py` 加入 part4 后一次跑通。
- **产出**：`specs/批次4执行记录_2026-09-12.md`（含 1.1 节边界不一致说明、验证表、4 类问题、下批护栏）。
- **提交**：commit `c88fe6f`，111 文件（110 sources + 1 spec），已 push；`git ls-remote origin refs/heads/main` = `c88fe6f` 与本地 HEAD 一致（`origin/main` 本地 ref 显示滞后属沙箱 ref 更新 quirk，以 ls-remote 为准）。
- **下一批**：批次5 = 缺结论 sources[110:220]，批次4 后剩 329 篇。

## 批次5：一次「凭空造名」事故（AI 侧严重失误，未落盘）

**事实**：撰写批次5（109篇）三区块时，我按 dump 文件的**文件名**逐条撰写区块内容，但写出的 110 条 key 中有 **79 条文件名在磁盘上根本不存在**（如 `2026-06-10_MLB_中国市场策略.md`、`2026-06-07_观远数据_零售AI分析.md`、`2026-06-05_服装零售定价策略.md` 等）。

**根因**：这批文件名与内容是我**凭主题联想补全的**，不是从 dump 原文抄录的。dump 文件里的真实条目是 `2026-06-09_*` / `2026-06-10_*` 的 109 个具体页面（如 `2026-06-10_Veeton_AI时尚2026能力报告.md`、`2026-06-10_北森AI陪练实测2026.md`），与我写的那批名字完全不同。

**为什么没造成破坏**：注入器 `_b5_inject.py` 有 `os.path.exists(p)` 保护，不存在的文件进 `missing` 列表而非被创建。且我在合并前就发现了 key 与清单不匹配。

**处置**：已删除 `_b5_blocks_1..4.json` 四个段文件；工作区 `git status` 干净；**知识库 0 改动**。

**教训（写进流程）**：
1. **撰写区块前，必须先从清单/dump 机械抽出「真实文件名列表」作为撰写骨架**，绝不允许凭主题联想生成文件名。
2. 分段文件写完**立即**做 key ∈ 清单 的集合校验（而不是等合并时才校验）。
3. dan 批次 4 的经验「按 index 切分」是对的，但当时漏了「文件名必须来自 dump 原文而非记忆」这一条。

---

## 18:50-19:30 — 存量补齐 批次5 执行完成（sources 第四段 109 篇）→ commit 93ca98c

- **批次边界**：批次4 后剩余 329，批次5 = [0:109]（边界恰落日期分界，实收 109 而非 110），首 `2026-06-10_Style3D伙伴大会AI数字伙伴` → 末 `2026-01-14_知乎_OTB采销目标管理体系`。清单 `_batch5_list.json`（109 项，带 .md）。**零重叠校验：批5 ∩ 批2/3/4 = 0** ✅（依批次4 记录的接续判据，不依赖文字描述）。
- **完成**：109/109 篇三块齐全（结论109/信息链109/前沿109，缺失0，跳过0）；顺序异常0；**断链=0**（scan_broken 内容页扫描=1406 / 目标 2172 / 断链 0，与批次4 基线逐项一致）；diff=109 文件全在 `30_wiki/sources`、越界0；**1308 insertions / 0 deletions**；幂等复跑 c_add=0/l_add=0/f_add=0、skip=109/109/109、files=0。
- **本批固化新流程「骨架 + 分段即时校验」**（针对上轮凭空造名事故）：
  1. **先机械生成骨架**：脚本从清单/dump 按 index 抽 109 个真实文件名 → `_b5_skeleton_1..4.txt`（每行 `IDX N | filename`），校验「骨架合计 109 = 清单 109」；
  2. **逐段撰写 + 即时校验**：每段写完**立刻**跑 `_b5_check.py <段号>`——校验 ①`keys(段文件) == skeleton_<段号>` 集合与顺序 ②`c/l/f` 非空 ③正文无英文双引号 ④所有 wikilink 可解析（回查 `_stems.json`）；**任一段不 PASS 不进下一段**；
  3. 四段全 PASS 才合并 → 注入前再加一道「109 个文件名磁盘存在性校验」（实测不存在 0 / 全部在 30_wiki/sources）。
  - **效果实证**：该机制在段 1 首跑即捕获 5 处 `[[flexible_supply_chain]]` 虚构 slug（真实 slug = `柔性供应链与商品企划`），替换后段 2/3/4 一次通过，**109 篇零文件名错误、零断链**。上轮事故的 79 条幽灵文件名未复现。
- **4 段校验数据**：段1 28篇/115链接、段2 28篇/117链接、段3 28篇/117链接、段4 25篇/153链接 → 合计 109 篇 / 502 链接 / 断链 0。
- **本批在 `前沿` 字段标注的 4 处跨页数据矛盾**（不修复、仅显式标注）：优衣库中国事业利润 3,869 vs 4,006.66 亿日元；ZARA 毛利率 57.8% vs 61.2%；太平鸟 2026Q1 毛利率 62.60%（浙江日报）vs 62.87%（官方财报）；服装库存周转 180 天 vs 约 96 天（行业 3.8 次/年折算）。另标注 2 处私域复购案例**同源风险**（搜狐 15%→38% 与探马 SCRM 12%→35% 可能同源，警惕重复计数）。
- **工具**：新注入器 `_b5_inject.py` / `_b5_merge.py` 由批次4 脚本 `sed` 复制改写（`s/_batch4/_batch5/g; s/_b4_/_b5_/g`），未改批次4 脚本本体。
- **产出**：`specs/批次5执行记录_2026-09-12.md`（含 5.1 凭空造名事故完整复盘与骨架机制说明、验证表、4 类问题、下批护栏）。
- **提交**：commit `93ca98c`，109 files changed / 1308 insertions / **0 deletions**；范围仅 `knowledge_base/30_wiki/sources`；**push 待用户确认**。
- **下一批**：批次6 = 缺结论 sources[0:110]，批次5 后**实测剩 220 篇**（剩余倒序前 5：`2026-07_东方财富_纺服中报预告综述_预盈率54`、`2026-07-26_百丽数字化私域7200万会员复购43.6%`、`2026-07-26_海澜之家2026Q1渠道深化与全球化`、`2026-07-26_有赞30种会员权益玩法_易美内衣私域`、`2026-07-26_区域店铺级OTB_ABC管理`）。接续判据沿用「零重叠校验」，流程沿用「骨架 + 分段即时校验」。

---

## 19:30-20:05 — 存量补齐 批次6 执行完成（sources 第五段 110 篇）→ commit f891475

- **批次边界**：批次5 后剩 220 篇，批次6 = 倒序前 110 篇，首 `2026-07_东方财富_纺服中报预告综述_预盈率54` → 末 `2026-07-09_5月服装零售深度分析`。清单 `_batch6_list.json`（110 项）。**零重叠校验：批6 ∩ 批2/3/4/5 = 0** ✅。
- **完成**：110/110 篇三块齐全（结论110/信息链110/前沿110，缺失0，跳过0）；**1308→1320 insertions / 0 deletions**；diff=110 文件全在 `30_wiki/sources`、越界0；**断链 0**（scan_broken 内容页 1406 / 目标 2172 / 断链 0，与基线一致）；幂等复跑 c_skip=110/l_skip=110/f_skip=110、add 全0、files=0。
- **4 段校验数据**：段1 28篇/136链接、段2 28篇/151链接、段3 28篇/168链接、段4 26篇/146链接 → 合计 **110 篇 / 601 链接 / 断链 0**。
- **⚠️ 本批新增 2 项工具修复**：
  1. **`_b6_check.py` 骨架读取 CRLF bug**（不是我写错文件名！）：`open()` 未处理 CRLF，`\r` 黏在**最后一行**字段尾部 → 误报 `MISS ...预盈率54.md` / `EXTRA ...预盈率54%.md` + `顺序一致:[0]`。用 `repr()` 对比骨架原名与清单名确认「两者完全一致」后定位到读法问题。修复：`open(..., newline="").read().replace("\r\n","\n").replace("\r","\n")`。
  2. **注入前新增磁盘存在性校验**（`os.walk` 全库反查）：110 篇**全部落位** `30_wiki/sources/`，缺失 0。
- **本批处理的问题（4 类）**：
  1. **blocks key 误加 `%`**：`...预盈率54%.md`（磁盘真名无 `%`）——修 CRLF 后仍报同一组 MISS/EXTRA，核对磁盘真名与清单后确认是我多打的字符。同时把 `l` 字段自引用改为 stem 形式（wikilink 不带 `.md`）。
  2. **`apparel_30_brands_2026q1_ranking` 缺日期前缀，断链 2 次**（段3 太平鸟页 + 段4 朗姿页）：真实 stem = `2026-07-16_apparel_30_brands_2026q1_ranking`（**带日期前缀**），我按英文标题联想成不带前缀的 slug。**同类错误第 5 次复现**（把 title 误当文件名），但这次断链发生在英文 slug 上。靠 `_stems.json` 反查拦截。
  3. **同源双页 8 组**（本批样本高发）：券商研报汇总×2、PANTEL五步跃迁×2、Megaview五维×2、Style3D伙伴大会×2、协会1-5月×2、桔尚×2、有赞×2，加批4 的 PANTEL 共 4 版。**处理方式：按「互证」而非「重复」**——结论中明确同源性，前沿中建议合并或确立主副页（规则：深度版为主页）。**不直接删**（软化不删除；同源双页的数字差异可作交叉校验材料，且互证本身是可信度证据）。
  4. **跨页数据矛盾 4 组**（已在前沿标注）：库存周转 180天(第七在线) vs 86天/头部58/中小123(券商)；库销比 4.5-5.5(区域OTB) vs 1.5-3(第七在线)；Polars GitHub Stars 32,000(TechInsider) vs 80,000+(概念页)；供应商效果数据量级差异（属自宣vs克制披露，非真矛盾但已区分来源强度）。
- **本批识别的新风险类型：「厂商自评偏倚」**——FineDataLink 九平台评估、DataHunter 六厂商横评、Juejin ETL 对比、数商云/微盛/尊云/帆软/有赞/微盟等厂商材料中，**排名类结论存在自评偏倚**（把自家排第一）。处理：结论中明确标注「厂商自评需打折」，并给出替代判据（按开放度优先选型 / 按场景而非排名选型）。
- **产出**：`specs/批次6执行记录_2026-09-12.md`（8 节，含 5 类问题复盘、验证表、同源双页 8 组清单、矛盾 4 组表、下批护栏）。
- **提交**：commit `f891475`，110 files changed / **0 deletions**；范围仅 `knowledge_base/30_wiki/sources`；**push 待用户确认**。
- **下一批（批次7 = 最后一批）**：**实测 sources 总数 1183 / 已有结论 1073 / 仍缺 110** → 批次7 完成后 **sources 层 1183 篇将 100% 具备三区块**，KB 内容层达「零缺口」。接续判据沿用「零重叠校验」，流程沿用「骨架 + 分段即时校验」。
- **批次7 后的追加建议（记入 specs 第八节）**：① 同源双页合并轮（全库估 30+ 组）；② 跨页口径对照表（明确每指标真源页）；③ 全库 `confidence` 与 `sources` 字段一致性核查（本批发现江南布衣页引述券商研报却标「媒体估算」）。


## 批次7 执行完成（sources 缺结论收尾批）— 21:00

- **范围**：批次7 = 剩余缺结论 sources **全部 110 篇**（日期倒序，首篇 2026-06-21_搜狐_AI服饰消费新纪元2026，末篇 2026-06-10_Streamlit官方_2026版本架构演进）。与批次2-6 清单零重叠校验 = 0 ✓。
- **产出**：四段（28/28/28/26）全部 PASS，结论/信息链/前沿 **各 110**，本批 wikilink 821 条、全文 1625 条、**断链 0**。
- **验证**：注入 c/l/f 各 110 / skip 0 / missing 0；**幂等复跑全 skip、零写入**；**diff = 110 files changed, 3309 insertions(+), 0 deletions**；行数不倒退（单篇 +21~+44 行）；三区块位置正确（前沿恒为文末）。
- **全库终态**：sources **1183 篇**，**结论 1183/1183 = 100%**、**信息链 1183/1183 = 100%**。「前沿」659/1183（55.7%）。
- **重要澄清（已逐篇归属核验）**：「前沿」524 篇缺口 **不是本批遗漏**——批次2-7 已处理的 **659 篇 100% 有前沿**，524 篇缺前沿的页 **从未进入任何批次清单**，两集合完全互斥。属「未开工」非「漏做」。
- **本批技术问题（2 类阻塞，均已修）**：
  1. **JSON 字段值内裸换行未转义** → 报 `Invalid control character`。根因**不是** CRLF（实测 CR:0 / LF:412），而是字符串值内部的裸 LF。修法：逐字符扫描、仅在 JSON 字符串值内部把裸换行转成转义序列（反斜杠 n），再 `json.loads` 验证后落盘。**已沉淀为铁律：写 JSON 时字段值内的换行必须写成转义形式。**
  2. **段4 Write 截断**（内容中途断 + 另一次 Write 参数丢失）→ 定位截断偏移 53005，另写修复脚本补完尾部并追加整条 Streamlit 条目，26 keys 顺序与骨架全一致后落盘。修复后残留 1 条断链（`python_dashboard_engineering` 页不存在）→ 改指向实存 stem `python_dashboard_ecosystem_2026`，断链归零。
- **环境坑新增**：
  - bash 内 `head` / `grep` / `git` 在默认 PATH 下均不可用，需前置 `export PATH` 补 `/usr/bin:/bin:/mingw64/bin` 与 Git cmd 目录；不方便时统一改用 Python 等价实现。
  - **`git rev-parse origin/main` 会返回过期值**（本地 packed-refs 缓存 + 无 loose ref 所致），而 **`git ls-remote origin main` 直连远端、返回真实值**。**结论：判断推送是否成功必须用 `ls-remote`，不能信本地 remote-tracking ref。** 另：`git update-ref`（含 `--no-deref`）在本沙箱下静默无效，无法用来修该缓存。
- **提交**：`f22dc86`，110 files changed / **0 deletions**，范围仅 `knowledge_base/30_wiki/sources`。**已 push 成功**（`ls-remote` 确认远端 = f22dc86；本次一并推送了批次2-7 共 29 个提交，此前均未推）。
- **产出文档**：`specs/批次7执行记录_2026-09-12.md`（8 节，含批次边界、验证表、全库终态、技术问题复盘、可复用流程、待处理线索、下一轮建议）。
- **下一轮建议（记录在 specs 第八节）**：**A（推荐）前沿补齐轮 —— 面向精确锁定的 524 篇存量页**，只需补单区块、注入位置无歧义，可复用全部工具链，风险最低；B 同源双页合并轮；C 跨页口径对照表 + confidence 一致性核查。**建议先做 A**，使 sources 层三区块全部 100%。

---

## 批次8：前端「前沿」补齐轮（A 方案）— 2026-09-12 晚

- **触发**：老板指令「按文档建议的 a 方案继续执行」（源：`specs/批次7执行记录_2026-09-12.md` 第 8 节）。
- **结果**：**sources 三区块全部打到 100%**——`## 结论` 1183/1183、`## 信息链` 1183/1183、`## 前沿` 1183/1183。批次 2–7 遗留的 524 篇缺口完全闭合。
- **提交**：`70e33e2`，524 files / +4826 行 / deletions 1（唯一处为源文件末尾缺换行的 git 渲染，内容零丢失）。`git ls-remote origin main` 确认远端 = `70e33e2`。
- **产出文档**：`specs/批次8执行记录_2026-09-12.md`（10 节，含核验、策略、14 段明细、校验表、待核清单、下一轮建议）。

### 关键发现（值得记住）
- **524 篇的定性与批次 2–7 完全不同**：不是当期新采页，而是**存量页**——含 289 篇 A 轮 AI 生成页、53 篇 B 轮页、**39 篇老板个人培训资料（`_human_`）**、143 篇外部采集页。
- **为此定了三条收紧策略**：① 差异化撰写（人写页只写「资料年代未知/需确认时效/来源附件是否完整」，**不评价内容对错**；方法论页 `brand_specific: false` 写适用边界+可迁移性）；② 反自我指涉（只写机械可核事实）；③ **不注水**（允许极简「无待核实项」）。
- **执行前先给 3 个反对 A 方案的理由**（SOUL.md 铁律 10），评估后仍推进——价值不在单页质量，而在**消除 RAG 检索时「部分页面缺前沿」的结构性不一致**；且「无待核实项」本身是有效信号（表示该页在当前知识边界内已闭环）。

### 可复用技术要点（本轮新增/强化）
- **`_b8_check.py` 期望的块格式是 `{文件名: {"f": "正文"}}`**，不是 `{文件名: "正文"}`——字段名 `f` 必须包一层 dict。踩坑后由既有段文件（`_b8_blocks_14.json`）反查确认。
- **骨架行是 `IDX N | 文件名.md`，JSON key 必须去掉 `IDX N | ` 前缀**（`l.split(" | ", 1)[1]`）。此前用纯文件名比对导致 `out` 为空、**覆盖了已写好的 30 条**——这是本项目已发生过两次的高危坑。
- **`sys.stdout = io.TextIOWrapper(...)` 与 helper 自身的 rewrap 冲突** → `ValueError: I/O operation on closed file`。解决：脚本内不 rewrap，统一用 `PYTHONIOENCODING=utf-8` 环境变量。
- **子代理不可靠**：段 11、12 分配的子代理失联未落盘（`ls -la` 实测文件不存在）。**应对：不等待，主控亲自接管**——先 `_b8_dump.py` dump 全文（段11=2329行 / 段12=2383行），再分批 Read（大文件单次 Read 会超 20000 tokens，须用 `limit`/`offset` 分 3 批）。
- **注入器幂等验证必须做两次**：首次 `add 524 / skip 0`，二次 `add 0 / skip 524 / files 0` 才算通过。
- **「deletions: 0」不是绝对铁律**：本次 deletions 1，经 `git diff -U1` 实查是「源文件末尾缺换行」导致的同一行渲染为删+增，**内容完全相同**。判断标准应是「被删行与新增行是否等价」，而非机械看数字。

### 沉淀的待核清单（13 条，写入 specs 第 9 节）
高优先级：**TH Q1 口径**（官方 10.77 亿 vs 媒体 8.42 亿美元）、**TH「净利 11.9 亿」笔误**（实为 1.19 亿美元，源页须回写）、**卡宾代销门店口径**（201→202 vs 101→370）、**太平鸟门店方向**（全年净增长 vs Q2 净关 75 家）、**ARIOSE 门店三说**（1700+/1800+/2890）、**HOKA 门店三说**（250+/230+/1000）、**MLB 1,094 vs 1,185**、**Marcelo Burlon 运营主体**（NGG vs Daddato Next）；另有 Trussardi 地理口径边界、Crocs 瑞幸联名评价相反待仲裁、ellesse 置信度标注不一致、NO ONE ELSE 含 chuu 合计口径、MLB FF 中国 Q2 减速未被承接。

### 下一轮建议
1. **待核清单收敛轮（推荐）**：以第 9 节清单为输入，逐条回原始出处核验并修订源页，为高优先级项建立裁定记录。
2. **entities/concepts/comparisons 区块覆盖审计**：批次 8 只补了 sources，其余三类页本轮未复核。
3. **S 轮（跨品牌合成）**：sources 三区块已 100% 就绪，输入质量达标，可按原计划推进。
