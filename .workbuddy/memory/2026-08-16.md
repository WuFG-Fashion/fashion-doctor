# 2026-08-16 工作日志

## Obsidian/结构/Git 三问评估（00:08）
- 用户三问：能否看到本地 Obsidian？LLM Wiki 结构是否合理？git 是否最新？要不要调整？
- **Obsidian**：vault = `knowledge_base/` 整个文件夹（.obsidian 在其内部），读到的 wiki 页 = 用户在 Obsidian 看到的内容，完全一致。配置齐全。
- **结构**：架构合理（Karpathy 分层 raw→sources→entities/concepts/practices/comparisons）。内容分布：sources 662 / entities 62 / concepts 60 / practices 12 / comparisons 5 / playbooks 9。comparisons 仅 5 页且全为早期手动建 → 印证"采集强、合成弱"，正是 S轮要补的缺口。
- **Git**：wiki 主体已同步（最新 5cb91c7，本地=origin/main）。但两个卫生问题：
  1. `.workbuddy/` 19 个文件被 git 跟踪（IDENTITY/SOUL/memory/heartbeat），尽管 .gitignore 第16行有 `.workbuddy/`——因规则添加前已提交，gitignore 对已跟踪文件无效。本地记忆会进公开仓库。需用户拍板：(a) 保留版本控制（MEMORY.md 项目约定有协作价值）vs (b) 按 gitignore 本意改本地私有（git rm --cached，不删本地）。
  2. 根目录 30 个一次性 `_*.py` 脚本未跟踪，悬在中间让 git status 很脏。建议可复用归入 scripts/ 提交，一次性删除或忽略。
- 状态：已把两个调整方向报给用户，等用户确认后执行（git rm --cached 安全，只解除跟踪不删本地文件）。

## 仓库卫生治理执行（00:15 完成，commit 145be54）
- 用户对问题1选(a)保留 .workbuddy 版本控制，问题2授权我执行。已落地：
- **.gitignore 调整**：移除 `.workbuddy/` 整体忽略 → 改为仅忽略瞬态子目录（`.workbuddy/tasks/`、`heartbeat/`、`logs/`）+ 新增忽略临时产出目录 `.kbtmp/`、`.scripts_tmp/`。memory/automations/skills 保留版本控制。
- **脚本归整**：34 个 KB 维护脚本（`_*.py` + contradiction_scan/fix_broken_links/kb_lint_5rules/update_benchmarks_controlled）从根目录移入 `scripts/`；5 个已跟踪产出文件（_pdca_data/_guide_*_result/_roundA_run4_*）移回 scripts/ 保持跟踪；纯未跟踪产出（_lint_result/_links_raw 等）移入 .kbtmp/ 被忽略。**业务分析脚本（guide_*/retail_*/店铺TOP3/明星导购等）未动**。
- **保持根目录**：6 个 `_automation_*.md` 规范文件留根目录（自动化 prompt 用绝对路径引用）。
- 提交 145be54（109 文件 / +6983），已 push origin main（5cb91c7..145be54）。其中 64 个 .workbuddy/memory 日记 + 4 个 automations 记忆文件首次纳入版本控制。脚本移动被 git 识别为 rename（100% 保留历史）。推送后 git status 干净。

## 知识库采集-A2轮执行（07:00，自动化 automation-1786773585569）
- A轮 A2（固定分组 12 品牌·品牌主体全维度）全量跑完：etudes/g_star_raw/hoka_one_one/humble_humble_r/karl_lagerfeld/king_baby/koyo/lacoste/levis/marcelo_burlon/mlb/mlb_kids。
- 产出：12 源页（`2026-08-16_A2_<brand>_全维度动态.md`，均含 结论+信息链+置信度+brand_specific+双链）+ 12 实体页追加「近期动态刷新」小节 + index/log/_health 更新。织网 ~30 双向双链、0 孤岛、0 新断链。
- 矛盾：1 处 ⚠️ —— mlb 中国门店数 1,094（2026末预计）vs 1,185（2026初·丰梵中国），口径差异待统一并回填 superseded_by。
- 决策偏差：未写 raw/（CLAUDE.md 标记只读）；git 用精确路径（§4.5）而非 spec 的 `git add knowledge_base/`；分两段提交 9deea9f + c061d8b 已 push。
- 下轮优先：koyo 实体边界补全（名称歧义）、mlb 门店口径统一、私牌补权威第三方数据源。
