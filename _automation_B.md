# B轮 执行规范（会员VIP + 导购 + 商品企划 · 品牌对齐版）

你是一个知识库自动维护者，负责 **B轮**。本轮覆盖 L2_03/会员与VIP运营、L2_04/导购能力评估、L2_05/商品企划。

⚠️ **本轮核心理念（品牌对齐）**：A轮已改为"以品牌为主体、全维度综合采集"，B轮的三个运营域（会员/导购/商品企划）本质上是**按品牌执行的运营动作**——太平鸟的会员体系与卡宾的完全不同。因此 B轮不再是纯通用主题搜索，而是**品牌上下文搜索 + 通用最佳实践**双轨并行，让运营知识与 A轮的品牌情报打通。

规则手册 = `knowledge_base/CLAUDE.md`，严格遵循（重点 2.3 区块规范与 5.1 质量门：每页含「结论」与「信息链」；2.4 置信度分级）。

## 品牌覆盖范围（品牌上下文搜索）

B轮的品牌搜索范围 = `kb_benchmarks.json` 的 `focus_brands`（当前 36 个）。每轮执行时：
1. **双核必搜**：cabbeen（卡宾）、peacebird（太平鸟）——每个 L2 域都必须搜这两个品牌的运营实践
2. **轮换重点**：从剩余 34 个品牌中，按 `kb_benchmarks.json` 顺序轮换 6-8 个（每轮不同，记录轮换位置），覆盖其运营动态
3. **通用最佳实践**：每个 L2 域同时搜 1 次行业通用最佳实践（不绑品牌）
4. 其余品牌若 A轮近期已采集相关运营信息，记"已有覆盖"后跳过，不重复

> 设计理由：B轮每 48 小时执行一次，36 品牌全量搜索不现实（上下文溢出风险同 A轮）。双核必搜保证核心品牌运营知识持续更新，轮换重点保证其余品牌周期性覆盖。

## 第零步（预检）：品牌运营覆盖缺口扫描

联网搜索前，先扫描知识库现状：
1. 读取 `kb_benchmarks.json` 的 `focus_brands`，检查每个品牌在 L2_03/04/05 三个域是否有对应的运营实践页或 source
2. 读取 `wiki/index.md` 与 `log.md`，找出 L2_03/04/05 中超过 14 天无新 source 的方向
3. 汇总为缺口清单（按"双核优先 > 长期未覆盖品牌 > 通用方法论"排序），在回复开头打印
4. 第二步 WebSearch 必须优先围绕缺口清单展开

## 第一步：加载上下文

1. 读 `knowledge_base/CLAUDE.md`
2. 读 `knowledge_base/wiki/index.md`
3. 读 `knowledge_base/wiki/log.md`
4. 读 `knowledge_base/kb_benchmarks.json`（获取 focus_brands 列表 + 会员/导购/商品企划已有基准）

## 第二步：联网搜索（品牌上下文 + 通用最佳实践 双轨）

### 2.1 L2_03 会员与VIP运营
- **品牌上下文**（双核 + 轮换品牌）：
  - `太平鸟 会员运营 复购策略 VIP 2025 2026`
  - `卡宾 会员 VIP 私域 复购 2025 2026`
  - 轮换品牌 × 会员运营（如 `Salomon 会员 忠诚度计划 2026`、`Crocs 会员 复购 2026` 等）
- **通用最佳实践**：`2026 会员运营 复购策略 私域 零售VIP 会员忠诚度`

### 2.2 L2_04 导购能力评估
- **品牌上下文**（双核 + 轮换品牌）：
  - `太平鸟 导购 培训 门店 业绩 2025 2026`
  - `卡宾 导购 店长 培训 零售人力 2025 2026`
  - 轮换品牌 × 导购（如 `MLB 导购 门店运营 2026`、`迪卡轩 导购 培训 2026` 等）
- **通用最佳实践**：`2026 导购培训 AI陪练 零售人力 业绩提升`

### 2.3 L2_05 商品企划
- **品牌上下文**（双核 + 轮换品牌）：
  - `太平鸟 商品企划 OTB SKU 品类 2025 2026`
  - `卡宾 商品企划 波段 品类结构 售罄率 2025 2026`
  - 轮换品牌 × 商品企划（如 `Lacoste 商品企划 品类 2026`、`艾诺丝 商品企划 2026` 等）
- **通用最佳实践**：`2026 OTB 商品企划 SKU管理 品类结构 售罄率`

⚠️ **品牌对齐硬性要求**：每个 L2 域的品牌上下文搜索至少覆盖双核（cabbeen + peacebird）；轮换品牌不得与上一轮完全重复（在 log.md 中记录本轮轮换的品牌列表）。若某品牌在 A轮近期已采集相同维度的运营信息，记"已有覆盖"后跳过，不重复造页。

## 第三步：质量审核（逐条过5关）

1. 有具体数据（数字/百分比） 2. 来源可信（拒绝营销软文） 3. 与服装零售相关 4. 时效2025-2026 5. 可操作

## 第四步：写入知识库（严格遵循 CLAUDE.md 3.2 + 2.3/5.1）

- 原始资料保存到 `raw/articles/YYYY-MM-DD_来源_主题.md`
- 编译到 `wiki/sources/` → `wiki/concepts/` → `wiki/practices/`
- ⚠️ 硬规则：每个新 `wiki/sources/` 页必须至少包含 1 条 `[[双链]]` 指向已有 concept 或 entity，禁止产生孤岛
- ⚠️ 每个新建/更新的 concept/practice 页必须含 `## 结论`（2-4 条合成洞察，是判断而非数据复述）与 `## 信息链`（上游来源 → 本页 → 下游实体/对比/打法 的双链推理链）
- 品牌上下文搜索产出的 source 页须双链到对应的品牌实体页（如 `[[cabbeen]]`、`[[peacebird]]`），打通 A轮品牌情报与 B轮运营实践
- 同步到 L2/L3 历史目录
- 更新 `wiki/index.md` 和 `wiki/log.md`

## 第五步：自动织网 kb-link（遵循 CLAUDE.md 3.5）

1. 扫描本轮新增的所有 `wiki/` 页面
2. 为每个新增页面找到可链接目标（同名实体 > 共用概念 > 同标签页面 > **同品牌**）
3. 在目标页面也加回链（双向链接）
4. 更新 `wiki/index.md`

## 第六步：矛盾检测（遵循 CLAUDE.md 3.4 第3条）

1. 提取本轮新增的 KPI 阈值/复购率/导购转化率/售罄率等关键数值
2. 与 `kb_benchmarks.json` 已有值交叉比对
3. 与品牌实体页中的运营数据交叉比对（如太平鸟门店数 vs 导购人数比例）
4. 如发现同一指标数值不一致，在新 source 页末尾加 `> ⚠️ **数据矛盾**：[指标] 在本轮为 X，但 [已有来源] 显示为 Y，待验证`
5. 输出矛盾清单（有则打印，无则打印 "✅ 无矛盾"）

## 第七步：Git 推送

```
git pull --ff-only || true
git add knowledge_base/ && git commit -m "[auto] Round B — L2_03/04/05 (品牌对齐·会员/导购/商品企划)" && git push
```

## 第八步：写日志 + 每日健康快照

1. 在 `knowledge_base/wiki/log.md` 追加：| YYYY-MM-DD HH:MM | ingestB | L2_03/04/05 — 采集X篇/织网X条/矛盾X处/品牌覆盖:双核+轮换X个 |
2. 生成每日健康快照并写入 `knowledge_base/_health/YYYY-MM-DD_daily_health.md`，内容须含：
   - 本轮：采集 X 篇 / 织网 X 条 / 矛盾 X 处 / 新增双链 X 条 / 孤岛数 / 新增「结论+信息链」页数
   - 品牌覆盖：双核 + 轮换品牌列表 + 下轮轮换建议
   - 第零步缺口清单与下轮优先方向
   - 若本轮无新增，明确标注"✅ 库已覆盖，仅补缺口/无重复造页"

## 第九步：置信度标注与上下文护栏（强制）

### 9.1 置信度（数据可信度分级，依据 CLAUDE.md 2.4）
- 每个新建 `wiki/sources/` 页 frontmatter 必须含 `confidence`（取值：财报 / 官方公告 / 第三方数据 / 品牌自宣 / 媒体估算），并在页内"来源链接"上方用 `> **置信度**：xxx` 显式声明。
- 更新 concept/practice 页时，关键数字（复购率/转化率/售罄率等）在正文内联标注，如 `复购率 42%（置信度：品牌自宣）`；凡"约/估"数据必须标 `（置信度：媒体估算）`。
- 矛盾检测（第六步）须优先比对同 `confidence` 等级数据；跨等级冲突以高等级为准。

### 9.2 上下文护栏
- **每品牌每域 WebSearch 上限 2 次**（含探针），超出即停止并记"已达检索上限"。
- **优先 WebSearch 摘要**，非必要不 WebFetch 整页。
- **双核 + 2 个轮换品牌写完后做一次中途 git commit**，将前半程落盘。
- 若执行至后半程察觉检索变浅/格式漂移，允许对剩余品牌仅做探针 + 记录"需复核"，**不得强行编造数据**。

### 9.3 分段提交
- 前半程（双核 + 2 轮换品牌写完）：`git pull --ff-only || true && git add knowledge_base/ && git commit -m "[auto] Round B — 前半程(双核+轮换)"`
- 后半程（剩余品牌 + 通用最佳实践写完）：`git pull --ff-only || true && git add knowledge_base/ && git commit -m "[auto] Round B — 后半程(轮换+通用)" && git push`
