---
type: source
title: Streamlit 2026版本架构演进（v1.53→v1.57）
tags: [streamlit, architecture, starlette, uvicorn, tornado, asgi, polars]
sources: [https://docs.streamlit.io/develop/quick-reference/release-notes/2026]
aliases: ["Streamlit", "2026版本架构演进（v1.53→v1.57）", "Streamlit 2026版本架构演进（v1.53→v1.57）"]
confidence: 第三方数据
brand_specific: false
created: 2026-06-10
updated: 2026-06-10
cross_refs: [[streamlit_dashboard_2026]], [[streamlit_production_dashboard]], [[polars_vs_pandas_2026]]
layer: T2
scope: public
volatility: fast
as_of: 2026-06-10
expires_at: 2026-09-08
status: active
---
# Streamlit 2026版本架构演进（v1.53→v1.57）

> **一句话摘要**：Streamlit 2026年5个版本完成Tornado→Starlette/Uvicorn架构迁移，新增st.bottom/st.menu_button/st.iframe等核心组件，并实现Polars零拷贝Arrow直传，是Streamlit历史上最大的架构升级。

> **来源**：Streamlit官方Release Notes
> **最后更新**：2026-06-10

## 核心要点

1. **架构革命**：v1.57默认使用Starlette/Uvicorn替代Tornado，获得ASGI兼容+现代Python异步生态
2. **Polars原生支持**：v1.57实现Polars DataFrame直接转Arrow，完全绕过pandas，性能和类型保真度双提升
3. **新组件三件套**：st.bottom（底部固定容器）、st.menu_button（下拉按钮）、st.iframe（嵌入外部内容）
4. **API现代化**：st.App程序化配置、st.App.secrets替代secrets.toml、st.logout身份登出
5. **废弃清理**：4个experimental API + 3个chart参数在2026年内完成移除

## 版本演进时间线

| 版本 | 日期 | 关键变更 |
|------|------|---------|
| v1.53.0 | 2026-01-14 | Starlette实验性引入、st.App/ASGI入口、st.logout、会话级缓存 |
| v1.54.0 | 2026-02 | st.logo支持Material图标、移除experimental_query_params和experimental_user |
| v1.55.0 | 2026-03 | 动态容器on_change、Widget bind参数、流式Markdown CSS颜色 |
| v1.56.0 | 2026-04 | st.menu_button、st.iframe、pandas 3.x支持、selectbox filter_mode |
| v1.57.0 | 2026-04-29 | **Starlette默认启用**、st.bottom、:shimmer[]、Polars Arrow直传 |

## 重大架构变更详述

### Tornado → Starlette/Uvicorn

**影响**：Streamlit从同步Web框架迁移到ASGI异步框架
- ASGI兼容：可与FastAPI/Starlette等现代Python Web框架集成
- 性能提升：利用Uvicorn的高并发事件循环
- 高级配置：通过`st.App`暴露底层服务器配置
- 自定义路由：支持HTTP中间件、生命周期钩子

**对现有项目的影响**：
- 如果使用`_get_websocket_headers()` → 改用`st.context.headers`
- 如果使用Tornado特定功能 → 需评估迁移
- 新部署默认使用Starlette，无需额外配置

### Polars Arrow零拷贝

```python
# v1.57前：Polars → Pandas → Streamlit（两次转换，性能损失）
df = pl_df.to_pandas()
st.dataframe(df)

# v1.57后：Polars → Arrow → Streamlit（零拷贝，性能提升）
st.dataframe(pl_df)  # 直接传Polars DataFrame
```

## 关键API变更对照

| 旧API（已废弃） | 新API | 废弃版本 |
|------|------|------|
| `st.experimental_get_query_params()` | `st.query_params` | v1.54 |
| `st.experimental_set_query_params()` | `st.query_params` | v1.54 |
| `st.experimental_user` | `st.user` | v1.54 |
| `_get_websocket_headers()` | `st.context.headers` | v1.57 |
| `st.plotly_chart(use_container_width=True)` | 用width参数替代 | v1.57 |

## 与服装零售系统的关联

对于已使用Streamlit构建多品牌看板（如[[peacebird]]和Cabbeen看板），v1.57升级建议：
- **st.bottom**：可用于固定品牌选择器或全局筛选条件
- **Polars Arrow直传**：大数据量销售明细表渲染性能显著提升
- **Starlette架构**：与现有Python后端（FastAPI等）集成更自然
- **st.menu_button**：品牌切换/设置菜单的更好实现方式

## 结论

本页是**Streamlit 官方 Release Notes 的版本汇总**，价值在于**用一份材料覆盖 2026 年 v1.53 到 v1.57 五个版本的全部关键变更**，**避免在多份分散记录之间来回拼装。**

**本次演进中影响最大的有两条。**

**第一条是架构从 Tornado 迁移到 Starlette / Uvicorn。****这不是一次普通的依赖替换，而是把 Streamlit 从「同步 Web 框架」推进到「ASGI 异步框架」**——**含义有三**：**一是可与 FastAPI / Starlette 等现代 Python Web 框架集成（ASGI 兼容）；****二是借助 Uvicorn 的高并发事件循环拿到性能收益；****三是通过 `st.App` 暴露底层服务器配置，支持 HTTP 中间件与生命周期钩子。****对本库这种已经用 FastAPI 做后端、Streamlit 做看板的组合，集成会自然得多。****但有迁移成本**：**仍在使用 `_get_websocket_headers()` 的代码必须改用 `st.context.headers`。**

**第二条是 Polars Arrow 零拷贝直传。****v1.57 之前是 Polars → Pandas → Streamlit 两次转换，既有性能损失又有类型失真；v1.57 之后 `st.dataframe(pl_df)` 可直接传入 Polars DataFrame，走 Arrow 零拷贝。****对本库的大数据量销售明细表渲染，这是最直接可用的性能提升点**（无需改口径、无需改数据层，只改渲染调用）。

**其余变更**：**新组件三件套**（`st.bottom` 底部固定容器、`st.menu_button` 下拉按钮、`st.iframe` 嵌入外部内容）；**API 现代化**（`st.App` 程序化配置、`st.App.secrets` 替代 `secrets.toml`、`st.logout` 身份登出）；**废弃清理**（4 个 experimental API 与 3 个 chart 参数在 2026 年内完成移除）。**其中 `st.bottom` 可用于固定品牌选择器或全局筛选条件，`st.menu_button` 适合品牌切换与设置菜单。**

**⚠️ 需注意 frontmatter 的 `volatility: fast` + `expires_at: 2026-09-08` 已被越过**（当前日期 2026-09-12）——**也就是说本页记录的 v1.57 已是「过期快照」**，**Streamlit 官方可能已发布更新的版本。****本页作为「架构演进脉络」的存档仍有长期价值（Tornado→Starlette 这一转变是历史事实，不会回退），但「当前最新版本号」这类信息必须重新核实后再用。**

## 信息链

[[streamlit_dashboard_2026]]、[[streamlit_production_dashboard]]、[[polars_vs_pandas_2026]]、[[python_dashboard_ecosystem_2026]]、[[peacebird]]、[[cabbeen]]

## 关联页面

- [[streamlit_dashboard_2026]] — Streamlit生产级最佳实践
- [[streamlit_production_dashboard]] — 多品牌看板构建实操
- [[polars_vs_pandas_2026]] — 三引擎性能对比
- [[python_dashboard_ecosystem_2026]] — 看板框架全景对比
- [[multi_brand_unified_analytics]] — 多品牌分析架构

## 前沿

指导 [[streamlit_dashboard_2026]] 与 [[streamlit_production_dashboard]] 的版本升级评估；Polars 直传性能收益与 [[polars_vs_pandas_2026]] 的三引擎对比互相印证；与同源的 Streamlit 全版本材料合并时须标注重复；版本号时效性需定期回访官方 Release Notes 复核。
