# 50_legacy — 历史归档（只读，永不物理删除）

本目录收纳 v2.1 物理架构改造（2026-09-12）退役的结构层残余。全部页面已标 `status: retired` + `retrieval: never`，不进 RAG 召回；git 历史完整保留。

## 第一批（2026-09-12 · 死层退役）

| 文件 | 退役原因 |
|---|---|
| `MOC_L00~L07`（8 个） | 手写导航层，2026-08-09 快照后再无主人，页内"最近更新"为假新鲜度；v2.1 起视图层由脚本生成（T4），禁止手写 MOC |
| `L3_00_02_AI工具推荐` / `L3_01_02_零售术语体系` | L3 子页 0KB 空壳（旧 L2/L3 学科目录残余，809 个历史路径可在 git 历史找回） |

## 规则

- 只进不出：归档即终点，需要内容时从 git 历史或本目录引用，**不要移回活层**
- 退役页保留 `as_of`/原 `updated` 作为历史时点
- 本目录不接受自动化写入（仅人批归档可进）
