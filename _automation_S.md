# S轮 执行规范（合成轮 · 跨品牌模式识别）

你是一个知识库自动维护者，负责 **S轮（Synthesis，合成轮）**。本轮**不采集任何新数据**，唯一任务是从已有 36 品牌知识中**提炼跨品牌模式**，让知识库从"数据库"进化为"知识系统"。

⚠️ **本轮核心理念（合成优于堆积）**：A/B/C 三轮都是"采集导向"（加新节点），S轮是唯一的"合成导向"（从已有节点中发现跨品牌模式）。第一性原理：100 条品牌情报不如 1 条跨品牌模式有价值。

规则手册 = `knowledge_base/CLAUDE.md`，严格遵循（重点 2.3 区块规范、2.4 置信度、2.5 数据生命周期、5.1 质量门）。

## 本轮覆盖范围

- **不搜新数据**：不调用 WebSearch / WebFetch，只读已有 wiki 页面
- **输入**：全部 36 个 focus_brands 的实体页 + `wiki/concepts/服装行业竞争格局.md` + `wiki/comparisons/` 已有对比页 + `wiki/sources/` 中近 30 天新增 source
- **输出**：`wiki/comparisons/` 新增/更新跨品牌对比页 + `wiki/concepts/服装行业竞争格局.md` 更新 + `wiki/sources/` 旧页 `superseded_by` 回填

## 第零步（预检）：合成缺口扫描

1. 读取 `kb_benchmarks.json` 的 `focus_brands` 列表
2. 读取 `wiki/comparisons/` 目录，列出已有对比页及其覆盖维度
3. 读取 `wiki/concepts/服装行业竞争格局.md`，检查其"竞争格局"部分的维度覆盖
4. 对比已覆盖维度与应覆盖维度，产出**合成缺口清单**（见下方"合成维度清单"），在回复开头打印
5. 优先处理缺口最大的维度

### 合成维度清单（应覆盖 vs 已覆盖）

| 维度 | 说明 | 数据来源 |
|------|------|----------|
| 营收规模分层 | 36 品牌按营收规模分层（百亿级/十亿级/亿级/千万级） | 实体页财务数据 |
| 增长模式分类 | 高增长/稳健/衰退/扭亏 四分法 | 实体页财务趋势 |
| 门店策略对比 | 直营 vs 加盟 vs 混合；开店 vs 关店趋势 | 实体页门店数据 |
| 毛利率分层 | 高端（>60%）/ 中端（40-60%）/ 大众（<40%） | 实体页财务数据 |
| 渠道结构对比 | 线上占比 / 线下占比 / 全渠道整合度 | 实体页渠道数据 |
| 国际化程度 | 纯国内 / 有出海 / 全球化 | 实体页渠道数据 |
| 品类定位 | 男装/女装/童装/运动/户外/街头 | 实体页 aliases/tags |
| 运营策略对比 | 会员体系/导购模式/商品企划方法（引用 B轮 practices） | B轮 practices 页 |
| 数据基建适配 | 各品牌数据结构与多品牌系统的适配度（引用 C轮 practices） | C轮 practices 页 |
| 风险信号 | 亏损/关店/库存积压/商誉减值等品牌风险汇总 | 实体页 + 矛盾检测记录 |

## 第一步：加载上下文

1. 读 `knowledge_base/CLAUDE.md`
2. 读 `knowledge_base/wiki/index.md`
3. 读 `knowledge_base/kb_benchmarks.json`
4. 读 `wiki/comparisons/` 目录（已有对比页清单）
5. 读 `wiki/concepts/服装行业竞争格局.md`

## 第二步：读取品牌实体页

按 `kb_benchmarks.json` 的 `focus_brands` 顺序，读取每个品牌的实体页（`wiki/entities/<brand>.md`），提取关键数据：
- 财务：营收 / 净利润 / 毛利率 / 同比增长
- 门店：总数 / 净增/净减 / 直营/加盟比
- 渠道：线上占比 / 线下占比 / 出海情况
- 运营：会员/导购/商品企划相关实践（如有）
- 风险：亏损/减值/关店/库存等负面信号

**上下文护栏**：36 品牌实体页内容较多，分 3 批读取（每批 12 个），每批读完即提取要点笔记，再读下一批。不得一次性全量读入。

## 第三步：跨品牌模式识别

基于第二步提取的数据，对每个合成维度执行模式识别：
1. **分层/分类**：将 36 品牌按该维度分组（如营收分层：百亿级 2 个 / 十亿级 5 个 / ...）
2. **识别模式**：从分组中提炼出 1-3 条可操作的洞察（如"高端户外品牌毛利率普遍 >55%，且增长主要来自中国市场"）
3. **识别异常**：找出不符合所属分组特征的品牌（如"某品牌营收十亿级但毛利率仅 30%，低于同级均值"）
4. **跨维度交叉**：尝试发现两个维度之间的相关性（如"高增长品牌普遍线上占比 >30%"）

## 第四步：写入知识库

### 4.1 新建/更新 comparisons/ 页

每个合成维度对应一个 comparison 页（如 `comparisons/营收规模分层_2026.md`）：
- frontmatter 必须含 `type: comparison`、`aliases`、`confidence: 第三方数据`（合成页的数据来自已有 source，confidence 取各 source 的最高等级）
- 必须含 `## 结论`（2-4 条跨品牌合成洞察）与 `## 信息链`（上游实体页 → 本页 → 下游应用）
- 必须含对比表格（品牌 × 维度值）
- 必须双链到相关实体页和 `[[服装行业竞争格局]]`
- 若已有同维度 comparison 页，**更新**而非新建（追加新数据 + 更新结论）

### 4.2 更新 服装行业竞争格局.md

- 将本轮识别的跨品牌模式写入 `## 竞争格局` 或 `## 行业趋势` 小节
- 新增/更新品牌间的对比关系双链

### 4.3 回填 superseded_by

- 扫描近 30 天新增 source，检查是否有同品牌同指标的旧 source
- 如有，在旧 source frontmatter 加 `superseded_by: "[[新source]]"`

## 第五步：自动织网

1. 扫描本轮新增/更新的所有 `wiki/comparisons/` 页面
2. 为每个新 comparison 页找到可链接的实体页和概念页
3. 在目标页面也加回链（双向链接）
4. 更新 `wiki/index.md`

## 第六步：Git 推送

```
git pull --ff-only || true
git add knowledge_base/ && git commit -m "[auto] Round S — 跨品牌模式识别（合成轮）" && git push
```

## 第七步：写日志 + 健康快照

1. 在 `knowledge_base/wiki/log.md` 追加：| YYYY-MM-DD HH:MM | ingestS | 合成轮 — 新建/更新X个comparison页/识别X条跨品牌模式/回填X条superseded_by |
2. 在 `knowledge_base/_health/YYYY-MM-DD_daily_health.md` 追加 S轮小节：
   - 本轮合成的维度数 / 新建 comparison 页数 / 更新 comparison 页数
   - 识别的跨品牌模式数 / 异常品牌数
   - 回填 superseded_by 数
   - 下轮建议关注的合成维度

## 与其他轮次的关系

| 轮次 | 方向 | 频率 | 品牌关系 |
|------|------|------|----------|
| A轮 | 采集（品牌在做什么） | 每日 3 轮 | 品牌主体 |
| B轮 | 采集（品牌怎么运营） | 每 48h | 品牌对齐 |
| C轮 | 采集（用什么工具分析） | 每 72h | 品牌感知 |
| **S轮** | **合成（品牌放在一起说明什么）** | **每周 1 次** | **跨品牌** |
| optimize | 维护（去重/修链/索引） | 每 6 天 | 品牌无关 |
